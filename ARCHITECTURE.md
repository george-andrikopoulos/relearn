# ARCHITECTURE.md — relearn

## Shape

A three-stage pipeline, with a typestate boundary between each stage so that later stages cannot receive earlier-stage data:

```
rules/*.md  ──parse──▶  Library<Unvalidated>  ──validate──▶  Library<Validated>  ──emit──▶  target files
                 │                                    │                              │
             ParseError                          ValidationError               io::Error
             (stops build)                       (stops build)                 (stops build)
```

No stage is permitted to skip a failing rule. A dropped rule is a lost correction.

## Crates / modules

| Module | Responsibility | Must NOT |
|---|---|---|
| `rule` | The neutral rule type and its newtypes (`RuleTag`, `ErrorClass`, `Incident`, `Recurrence`, `Home`, `Status`). Parsing **and** serialization (`parse_document` / `to_document`, inverses) live here. | Know anything about output formats |
| `library` | Collection semantics: uniqueness of tags, home partitioning, the typestate (`Unvalidated`/`Validated`) | Perform I/O |
| `emit` | One submodule per target: `claude`, `cursor`, `copilot`, `agents`, `claude_rules`. Pure functions `&Library<Validated> -> Vec<OutputFile>`. Shared scope/ordering/status helpers (`LoadSemantics`, `Globs`, `home_rank`, `HomeSlug`, `emittable`, `enforcement_note`, `recurrence_note`) | Read the filesystem, or read anything not carried by the rule |
| `lint` | Advisory static analysis over `&Library<Validated>` → `Vec<Finding>`: overlapping scope, home-slug collision, dangling references, retired references, unheld recurrences. Reports only; findings carry a `Severity` (`Info`/`Warning`/`Error`). | Modify or delete rules, or perform I/O |
| `pull` | What a bulk pull would do, worked out before it does any of it: `Plan` over a local library and an upstream one, sorted into take / refresh / drop / left-alone-with-a-reason | Perform I/O, or drop anything that is not a cache |
| `poke` | The federation's signal, surfaced inside `lint`: the five triggers over a local library, an upstream library and an `Aggregate`, returning `Pokes` (what to say, and how many were withheld by the cap) | Perform I/O, carry a severity, or be reachable from `build` |
| `fsio` | All filesystem reads and writes, including the generated-file guard (write side), the drift-detection verify (read side), and the two rule-file writers — `write_rule` for source this install owns, `write_cache` for a copy it does not | Contain business logic, or let either rule-file writer accept the other's subject |
| `cli` | Argument parsing, command dispatch (`check`/`build`/`list`/`lint`/`verify`, and the federation flows `pull`/`adopt`/`contribute`/`report`/`aggregate`), human-readable diagnostics | Contain business logic |

The dependency direction is strictly `cli → library → rule` and `cli → emit → library`; `lint → {library, emit, rule}` (it reuses `emit::HomeSlug` because a slug collision is defined by emit's output paths); `poke → {library, rule, aggregate, report}` and `pull → {library, rule}`, and **nothing depends on either** but `cli`; `fsio` is a leaf used by `cli` only. `emit` never touches `fsio` — emitters return values, the caller writes them. This is what makes emitters trivially testable. The `lint` and `poke` modules are pure and never write: a flagged rule is input to a human decision, never auto-deleted, and a poke is news a person acts on. Two arrows are **forbidden rather than merely absent**, and a source-level scan asserts each: no emitter or build-path file may reference `aggregate`, and none may reference `poke` — a build that behaved differently for having seen a downloaded file, or for having been poked, would make the instruction layer depend on what a clone happened to contain.

## The neutral rule format

```
+++
tag        = "R:parse-wide-then-range-check"
title      = "Parse wide, then range-check"
error_class = "Range-validating by parsing into the target narrow type, making the out-of-range case unreachable"
home       = { kind = "domain", name = "rust" }
created    = "2026-07-23"     # quoted string, parsed by Date::parse (see decisions log)
origin     = "mined"       # mined | codified | mandated -- the counter-metric to recurrence
status     = { kind = "active" }
incident   = "Grouping task 01: 5/5 samples parsed into u16, so 70000 returned NotANumber; OutOfRange unreachable."
+++

Parse into a type wide enough to *represent* the out-of-range value, then range-check
to mint the narrow newtype. The perimeter must be able to see the illegal value in
order to name it illegal.
```

`status` is a sum type, so a graduated or atticked rule carries its destination:
`{ kind = "graduated", to = "hook:no-narrow-parse" }`, `{ kind = "attic", reason = "...", date = "2026-09-01" }`.

`origin` carries a payload for two of its three values, written as a sibling key rather than
nested — `approval = { by = "...", date = "...", control = "..." }` for a mandate, and for a
codified rule an optional artefact:

```
origin = "codified"
source = "Dmitry Vyukov, Intrusive MPSC node-based queue, 1024cores"
```

Both are refused on an origin that has no use for them, rather than ignored. `source` is optional
and absent is the common case, so no rule file needed editing when it was added; a mandate's
approval is required, because a mandate with no signer is an unenforced guarantee.

A rule that has fired **again** since it was written records each later occurrence as a
`[[recurrence]]` table, after every scalar key (a TOML array of tables captures everything
that follows it):

```
incident   = "the original, unchanged"

[[recurrence]]
date     = "2026-08-30"
incident = "Fired again: the domain-knowledge section still carried the old derivation."
```

The field is optional and absent means "has not recurred", so no rule file needed editing
when it was added — asserted byte-exactly over the whole committed corpus by
`tests/corpus.rs`.

## Type-driven decisions

- **`RuleTag`** is a newtype minted only by `RuleTag::parse`, which enforces the `R:[a-z0-9][a-z0-9-]*` shape. Nothing downstream re-checks; possessing a `RuleTag` *is* the proof.
- **`Home`** is an enum (`Global | Domain { name } | Project { path }`). One home per rule is a type-level guarantee, not a lint.
- **`Status`** is an enum carrying its payload, so "graduated" without a destination, or "attic" without a reason, cannot be constructed.
- **`Status::emittability`** is the single type home of the emit-status policy: an *exhaustive* match producing `Emittability::{Emit, Suppress}`, so a future status variant cannot compile until its emit policy is decided (rather than defaulting to "emitted" and silently leaking). Every emitter filters through `emit::emittable`, which reads it — the policy is applied in one place, not re-derived five times.
- **`Origin`** is `Mined | Codified`, mandatory, and is the counter-metric to recurrence: `Rule::is_inert` is `codified && never recurred`. A defaulted origin would classify unlabelled rules silently, which is the under-reporting the counter exists to detect.
- **`Status::Graduated` carries a `date`**, so "did this recur *since* it graduated?" is answerable at all — the question that separates a rule wanting promotion (`Warning`) from a stronger control that demonstrably did not hold (`Error`).
- **`Status::prose_coverage` and `Status::whole_class_claim`** are the two type homes of the recurrence-and-citation policy, both *exhaustive* matches on `Status`. `prose_coverage` answers "is prose still carrying any of this?" (`Active` and `Partial` → `Holds`) and `whole_class_claim` answers "does a named control claim *all* of it?" (only `Graduated`). Their asymmetry is what makes `Partial` safe: a recurrence in a partly-held rule is reported as a `Warning` against the prose and never as the `Error` that says a control lied. Both replaced `matches!(status, Status::Active)` at the call site, which would have decided a new variant's behaviour **silently** — and wrongly, in both directions at once.
- **`Recurrence`** is the pair `(Date, Incident)`, not a bare `Incident` and not a second element of an `incidents` list. The triggering incident's date is the rule's `created`; a recurrence has no such field, so it carries its own. A single list would make `incidents[0]` mean something no other element means — a shape that lies about itself.
- **`Rule::new` takes `recurrences` as a required ninth argument**, not a defaulted field or a setter, so *dropping* a rule's recurrence history is a compile error at the site that drops it rather than a silent loss. Same reasoning as "a parse failure stops the build": the fact easiest to lose is the one that must be impossible to lose quietly.
- **Typestate on `Library<S>`** — `emit` accepts only `Library<Validated>`, so emitting unvalidated rules does not compile.
- **Parse wide, then range-check** applies to date fields: parse into a full date type, then constrain, so an out-of-range date reports as out-of-range rather than as a syntax error. (Dogfooding `[R:parse-wide-then-range-check]`.)

## Emission targets

| Target | Output | Notes |
|---|---|---|
| `claude` | `<out>/skills/<home>/SKILL.md` | **One skill per home layer** (not per rule — decision 2026-08-13). `<name>` is the home; `description` aggregates that home's rules for trigger coverage |
| `cursor` | `<out>/.cursor/rules/<tag-body>.mdc` | One `.mdc` per rule (Cursor's native granularity). Filename is the tag **body** (`R:foo` → `foo.mdc`) — the full tag's `:` is not a valid filename character everywhere. Front-matter `description`, `globs`, `alwaysApply` — **`globs`/`alwaysApply` derived from `Home`** via the shared `LoadSemantics` helper (domain→pattern table) in `emit`, never carried on the rule (decision 2026-08-13). Values are **not** YAML-quoted: Cursor parses `.mdc` front-matter leniently and `globs` holds a raw glob list |
| `copilot` | `<out>/.github/copilot-instructions.md` | Single concatenated file; rules ordered by home then tag |
| `agents` | `<out>/AGENTS.md` | Single concatenated file |
| `claude_rules` | `<out>/.claude/rules/<home-slug>.md` | **One file per home the target can scope** — `Home::Project` (unscoped, always resident) and known-language `Home::Domain` (scoped with `paths:`); an unknown-language domain and `Home::Global` are excluded (decision 2026-08-22). Path **derived from the home** via `HomeSlug` — decision 2026-08-22. Deliberately *not* `<out>/CLAUDE.md`: that path is the hand-authored project charter by Claude Code convention, and claiming it was a two-owner collision. A repository's own `CLAUDE.md` pulls its layer in with `@.claude/rules/<home-slug>.md` |

Every emitted file carries a generated-by header naming the source rule(s) and a hash, which is what the overwrite guard checks.

**Recurrence annotation (all targets).** A rule carrying recurrences is emitted with a
`> Has recurred <n> time(s) since it was written; most recently <date>.` blockquote under
its heading, rendered by `emit::recurrence_note` and spliced beside the graduation note.
The two are independent facts — a graduated rule that has also recurred carries both.

**Emit-status filtering (all targets).** Every emitter draws from `emit::emittable(library)`, not the raw rule set: `active` and `graduated` rules are written, `attic` rules are suppressed (a retired rule must never be written into an active instruction file). A graduated rule is emitted **annotated** — a `> Also enforced by <destination>.` blockquote under its heading — because the instruction layer tunes generation *before* it happens, a control distinct from the stronger layer that catches *after*, so the rule keeps its tuning job while flagging that a hook also holds it. The full library (atticked rules included) stays available to `lint`, which needs retired rules present to flag references to them.

## Decisions log

| Date | Decision | Why | Rejected alternative |
|---|---|---|---|
| 2026-08-13 | TOML front-matter, `+++` delimiters | `toml` crate is well maintained in Rust; YAML's Rust ecosystem is fragmented (`serde_yaml` unmaintained) | YAML front-matter — more conventional, worse maintained |
| 2026-08-13 | One file per rule, not one library file | Diffable, individually attributable, survives concurrent edits; matches "one home per rule" physically | Single `rules.toml` — simpler parse, worse provenance |
| 2026-08-13 | Emitters are pure, `fsio` writes | Makes every emitter unit-testable without a filesystem; keeps the destructive operation in one audited place | Emitters write directly — fewer types, untestable |
| 2026-08-13 | Parse failure stops the build | A silently skipped rule is a lost correction — the exact failure this project exists to prevent | Warn and continue — friendlier, unsafe |
| 2026-08-13 | Provenance fields are mandatory | The governance argument rests on provenance; optional provenance makes the tool complicit in sediment | Optional fields with defaults |
| 2026-08-13 | Type-Driven Design, not test-first | Standing doctrine (`rust-typedd`): a type constrains the whole space, a test samples points; in an AI-assisted pipeline the compiler is the cheapest retirement stage | TDD-first |
| 2026-08-13 | Claude skill emitter is one skill **per home layer**, not per rule | The skill *description* is always-resident metadata (P6, context is finite); per-rule grows it `O(rules)` and this library is built to grow. Matches the existing hand-authored skills (`project-discipline`, `rust-typedd`) which bundle many rules by domain; domain-level triggering is the right granularity. Future escape hatch: an optional `skill_group` field only if one home ever needs more than one skill. | One skill per rule — precise triggers, but unbounded resident-metadata growth |
| 2026-08-13 | Cursor `globs`/`alwaysApply` are **derived from `Home`**, not carried as target-specific fields | `Home` already *is* the neutral scoping concept; translating it into Cursor's vocabulary is the emitter's job, and vendor knowledge belongs in the emitter. Rule-level `cursor.*` fields would fragment the neutral format (every vendor then wants its own), contradicting P2. If finer scope than `Home` expresses is ever needed, enrich `Home` so every emitter benefits — never add a Cursor-only field. | Optional `cursor.*` fields on the rule — flexible, but rots the neutral format into a union of vendor front-matters |
| 2026-08-13 | Dates in front-matter are **quoted strings**, parsed by `Date::parse`, not TOML's native date literal | Preserves the `[R:parse-wide-then-range-check]` dogfood and the "out-of-range dates report as out-of-range" feature end to end: an impossible date (`2026-13-01`) must surface as `MonthOutOfRange(13)`, but a native TOML date would reject it as a *syntax* error first, collapsing the class. A quoted string reaches `Date::parse` intact. | Native TOML dates — nicer syntax, but defeats the dogfood and couples the range semantics to the TOML crate |
| 2026-08-13 | The generated-by header (marker + version + source tags + `sha256` of the body) is rendered and applied by **`fsio` at write time**, not by the emitters | Keeps every emitter a pure `&Library<Validated> → Vec<OutputFile>` body-producer with no hashing concern, and centralizes the marker format in the one module (`fsio`) that also reads it back — the writer and the overwrite-guard checker cannot drift. The emitter still supplies provenance via `OutputFile.sources`; `fsio` only serializes it. Guard placement: an HTML comment **after** the body, valid in every current (markdown-family) target and never disturbing the leading YAML front-matter of a skill or `.mdc`. | Emitters embed the header — spreads the marker format across five emitters, couples each to `sha2`, and forces the checker to trust five independent renderers to stay byte-identical |
| 2026-08-13 | The write guard is **all-or-nothing** via a pre-flight pass: check every target for the marker first, abort the whole write before touching disk if any existing target is unversioned | A single human-authored collision must never leave a half-generated tree; refusing up front is the safe failure. Matches "a dropped rule is a lost correction" applied to the write side — do not destroy unversioned content. | Write-as-you-go, error on first conflict — simpler, but leaves partial output behind |
| 2026-08-13 | **Contradiction detection is a Claude review pass** (a governance process-control), not a `lint` code check | Contradiction is a semantic judgment. Per the model-selection doctrine, intelligence-needing work is delegated to Claude through the subscription; a keyword heuristic in the linter would be a probabilistic guess sold as a deterministic guarantee — exactly the sediment the tool fights. The corpus (`rules/*.md`) is readable; Claude judges consistency periodically (first pass 2026-08-13, clean). | A keyword/similarity heuristic in `lint` — deterministic-looking, but dishonest and prone to both false positives and misses |
| 2026-08-13 | **`emit` filters by status: suppress `attic`, emit `active` + `graduated` (graduated annotated with its destination)** | A retired rule written into a live instruction file would *instruct withdrawn guidance* — the sentinel-leak failure class, one home. Graduated rules are kept, not dropped, because the instruction layer shapes generation *before* it happens whereas the graduated-to hook only catches *after*; both raise first-time-right (the metric the papers optimize), so a graduated rule keeps its tuning job — annotated so a reader knows a stronger control also holds it. Policy lives in `Status::emittability` (exhaustive match → a new status must decide) and is applied once in `emit::emittable`. | Emit every validated rule regardless of status (leaks retired guidance); or suppress `graduated` too (loses the pre-generation tuning a hook can't provide) |
| 2026-08-22 | **The project layer emits to `.claude/rules/<home-slug>.md`, one file per project home, with the path derived from `Home` — not to the constant `CLAUDE.md`** | Two defects, one root cause. (a) *The collision.* `<out>/CLAUDE.md` is the hand-authored project charter by Claude Code convention, so the emitter and a human owned one path. Bare `relearn verify` therefore exited non-zero on a clean tree and the green invocation named four of five targets — a gate whose default fails teaches every reader to add a flag, which is how a control degrades into decoration. The write-side marker guard had absorbed this correctly for months, and that is exactly why nobody looked: a guard that never fails visibly is indistinguishable from a guard that is not needed (`[R:prefer-by-construction]`; Paper 3 §8 uses this repo as the worked example). (b) *Home leakage*, found while fixing (a) and not previously recorded: the emitter filtered `Home::Project { .. }` without distinguishing *which* project, so all project homes were concatenated into one file and relearn's project layer carried stochos-lab's rules — a P2 violation in the emission layer. Deriving the path from `HomeSlug` (shape `[a-z0-9-]+` by construction) fixes both at once and leaves the emitter no free-form path with which to name a conventionally hand-authored file, so the collision is unrepresentable rather than avoided. The data needed came from the rule's own `Home`, so the "emitters read only what the rule carries" invariant is untouched. | *A different constant* (e.g. `PROJECT-RULES.md`) — swaps one collidable literal for another and leaves the leakage. *Making `Unversioned` non-fatal in `verify`* so the collision stops reporting — treats the symptom, and genuinely weakens the gate: a generated file replaced by hand-authored content would stop being reported at all. *Dropping `claude-rules` from the default target set* — hides a design decision in a default value, where the next reader will not see it |
| 2026-08-22 | **The emitted tree is committed, and CI runs `relearn verify` bare** | `verify` is the ledger's flagship gate and nothing invoked it: CI ran `fmt`/`clippy`/`test` only, and `git ls-files` returned nothing under `skills/`, `.cursor/`, `AGENTS.md` or the Copilot file, so a clean clone had no generated files for it to check. The artefact was real, correctly named and well tested, and its default invocation ran nowhere on a tree that could not have satisfied it. Paper 3 §9 names this as the sharpest limit of the enforced-by format — *the column records enforcement, not invocation* — and proposes recording the invocation and requiring it be the one CI runs. This is that, applied to the repository the paper uses as its case. Committing the tree is what gives `verify` something to verify; running it **bare** is what keeps the default path the operative one. | *Leaving the tree untracked and verifying a freshly-built tree in CI* — passes trivially, since `build` then `verify` in the same job only proves emission is deterministic (already property-tested) and would never catch the hand-edit or staleness the row claims |
| 2026-08-22 | **EOL pinned to LF for every emitted path in `.gitattributes`** | `verify` recomputes each file's body `sha256` and compares it to the hash in the file's own header. Committing the tree therefore makes line endings load-bearing: with `core.autocrlf=true` (this machine's setting, and the GitHub Actions Windows runner default) a checkout rewrites LF to CRLF, the body hashes differently, and every generated file reports `HandEdited` — green on Linux, red on Windows, for a reason nothing in the error message names. Pinned structurally rather than by teaching the hash to normalise, because the bytes on disk are what the guarantee is about (`[R:pin-eol-for-executable-text]`). | *Normalising line endings inside `render_with_header` before hashing* — makes the hash forgive a class of real edit, and hides the platform difference instead of removing it |
| 2026-08-13 | **Drift detection (`relearn verify`) compares each generated file to a fresh emission, classifying integrity *before* freshness** | "Emitted files are never sources" is a claim worth *verifying*, not just preventing. The header already hashes the **body only**, so a file's integrity is checkable against itself (body-hash vs recorded hash → `HandEdited`) with no rules needed; freshness (self-consistent but ≠ fresh emission → `Stale`) needs the rules. Checking integrity first means a hand edit is never mislabelled as mere staleness. `verify` and `build` share `cli::emit_selected`, so they cannot disagree on the expected set. Read-only: the write guard *prevents* clobbering, `verify` *detects* drift — complementary, not overlapping. | Byte-compare only (can't tell a hand edit from a stale rebuild); or re-hash without re-emitting (misses staleness vs the current rules) |
| 2026-08-22 | **`Scope` becomes the three-variant `LoadSemantics`, and the rules layer emits known-language domains scoped with `paths:`** | The pair `(globs: Vec<String>, always_apply: bool)` carried three real load models in two fields, so a fourth combination was representable and every emitter had to re-derive which of the three it held. Worse, a target that could not express one of them had no way to say so — it rendered something plausible. That is not hypothetical: `.claude/rules/` front-matter is **two-state** (absent → always; `paths:` → on reading a match), and an empty glob list has no third meaning there. Measured 2026-08-22 with an `InstructionsLoaded` hook (method in `docs/load-semantics-measurement.md`), `paths: []` loads at `session_start` — so rendering a no-glob domain "unscoped" would have promoted a rule meant to be *asked for* into one resident in every session on the machine, silently. As variants (`Always`, `WhenReading(Globs)`, `OnRequest`) with `Globs` non-empty by construction, the state cannot be built, a target that cannot represent a case must match it and decide, and a fourth load model added later is a compile error at every emitter. Cursor can say all three and does; the rules layer says two and **skips** `OnRequest`, which keeps reaching the assistant through the skill target — the mechanism that actually implements "by description". This is what makes route (c) of the `rust-typedd` homing decision real: `domain-rust` now emits to `.claude/rules/domain-rust.md` with `paths: ["**/*.rs"]`, one home, loading when Rust is touched. | *Keep the pair and special-case the empty list per emitter* — the same three-states-in-two-fields defect, now with the divergence spread across five call sites. *Emit `paths: []` for an unknown domain* — reads as "matches nothing" and behaves as "matches everything"; measured, not assumed. *Emit `Global` here too* — it is `Always` and would render fine, but it already has an always-resident home, so this would put one rule in two Claude files, which is the duplication the tool exists to prevent |
| 2026-08-22 | **`build` / `verify` take `--home <slug>`; the filter lives on `Library<Validated>` and skips re-validation** | Route (c) needs the *domain* layer emitted into `~/.claude/rules/` and nothing else. Without a filter the only way to emit that layer is `--targets claude-rules`, which also writes every **project** layer — and project homes are `LoadSemantics::Always`, so `project-relearn.md` and `project-stochos-lab.md` landing in a user scope would load unscoped in every session in every repository. That is the same promotion-to-always-on defect the `LoadSemantics` work exists to prevent, arriving through the CLI instead of the emitter. The filter is a predicate on `Library<Validated>` rather than a `Home`-shaped argument, so `library` gains no dependency on `emit`'s slug vocabulary and the layering holds. It does **not** re-validate: the only cross-rule invariant is tag uniqueness, and a subset of a set with unique tags still has unique tags, so the witness carries over by construction — stated in the doc comment beside the code, because a future invariant that does *not* survive subsetting would not be caught by the compiler. An unknown slug is an error naming the known ones, not an empty emission: `list --home` may print nothing because printing nothing is its answer, but a build that writes nothing and a `verify` that checks nothing both *report success*, which is the failure class this repository exists to catch. | *Hand-copy the emitted file into `~/.claude`* — unmanaged, no freshness gate, goes stale silently, and puts an unversioned file in a relearn-owned directory. *Accept the project layers in the user scope* — promotes project rules to machine-wide, the defect itself. *Filter the emitted `OutputFile`s by path* — works only for per-home targets and re-derives home membership from a string, exactly the sort of incidental coupling `HomeSlug` was introduced to remove |
| 2026-08-24 | **`copilot-pack/` is a second emission of the copilot target, generated and CI-verified — not a hand-assembled bundle** | The pack exists so the folder can be lifted out and dropped into an unrelated repository whose environment has no Rust toolchain and cannot run `relearn build`. A distributed copy of generated content is a staleness hazard by construction: the moment a rule changes, the copy is a confident, well-formatted lie about the library. Emitting it through the same `emit::copilot::emit` keeps it a build artefact — it carries the generated-by marker, so a hand edit hashes wrong, and `verify --targets copilot --out copilot-pack` in CI fails the build when a rule change was not rebuilt into it. The pack lies outside every relearn-owned path, so bare `verify` cannot see it and the second CI step is not redundancy but the only thing that reaches it; `.gitattributes` pins `copilot-pack/**` to LF for the same reason the root tree is pinned. The README beside it stays hand-authored and is recorded in FEATURES.md as the unenforced half — it asserts counts nothing checks. | *Commit a hand-written copy of the instructions* — no marker, no hash, no gate; goes stale on the next rule with nothing to notice. *Point users at the root `.github/copilot-instructions.md`* — no install instructions travel with it, and a single raw file is not a folder anyone can download whole. *Generate the README's numbers too* — the right end state, but it needs a rendering path the tool does not have; left open in TODO.md rather than faked |
| 2026-09-06 | **A recurrence is a separate `Vec<Recurrence>` field, not a second element of an `incidents` list** | The rule already recorded the incident that *created* it and nothing about the incident that *followed* it, so a recurring error class left two bad options: edit the original `incident`, destroying the record of what the rule was written for, or write a second rule, which is the P2 violation this tool exists to prevent. Either way recurrence — the one observation that says whether a rule is working — was lost. The two are not the same kind of thing: the first incident is *why the rule exists* and its date is `created`; a recurrence is *evidence the rule failed* and has no `created` to borrow, so it carries its own date. A single list would make `incidents[0]` mean something no other element means. A separate optional field is also backward compatible by construction: absent parses as empty, renders nothing, and no rule file needed editing — asserted byte-exactly over all forty-six by `tests/corpus.rs`, which is what turns "no migration needed" from a hope into a fact | *`incidents: Vec<Incident>`* — one field instead of two, but the first element silently means something different from the rest, and the recurrence dates have nowhere to live. *A `Default` field or a `with_recurrences` setter* — spares nineteen call sites the `Vec::new()`, at the cost of making a dropped recurrence history silent in the parser or in any future transform |
| 2026-09-06 | **`UnheldRecurrence` is `Warning`, with no count threshold, and does not fire for a graduated rule** | *Warning, not Error*: `Error` in this lint means the emitted tree would be wrong — two rules colliding on one output file — and a build that emits correctly should not fail. An unheld recurrence is a fault in the **library**, which is a different thing. *No threshold*: the first recurrence already proves the prose failed, two is not more actionable than one, and a cut-off would be a magic number this codebase does not use. *Not for graduated rules*: a recurrence **after** a graduation is the sharper finding — the named stronger control was claimed to hold this and demonstrably did not, a lying artefact and an `Error` rather than a `Warning` — but it needs a graduation **date**, which `Status::Graduated { to }` does not carry; adding one touches status parsing and serialisation for every rule file. Flagging it here as an ordinary `UnheldRecurrence` would say the wrong thing (the rule is *not* held by prose alone) and pre-empt the finding it deserves | *`Error` severity* — fails a build whose output is correct, and teaches readers to pass a flag, which is how a gate degrades into decoration. *A `>= 2` threshold* — invents a number nothing justifies. *One finding covering both statuses* — merges two findings with different severities and different remedies into one that is accurate for neither |
| 2026-09-06 | **The recurrence metric ships with its counter-metric recorded as `NOTHING YET — exposed`, not computed** | P5 requires every metric to carry a counter-metric, and this one's gaming path is plain: recurrences are recorded only by someone willing to write down that their own rule failed, so the number falls through under-reporting exactly as easily as through prevention. The counter is the corpus's *inert* fraction — rules that never recurred **and** were never mined from a real failure — and the null result says those are the rules that change nothing (*"you cannot author your way to a delta"*), so a library that looks healthy because it is full of them is the failure this metric would hide. It cannot be computed today: codification-dating is a prose convention, not a field, and making it one needs an `origin: Mined \| Codified` — a second modelled fact with its own argument. Using the exposure mechanism is the honest alternative to either violating P5 or doubling the scope | *Inventing `origin` in this change* — doubles the scope and settles a modelling question in passing. *Shipping the metric unaccompanied* — violates P5 silently, in the repository whose subject is not doing that |
| 2026-09-06 | **`relearn lint --deny <warning\|error>`, defaulting to `warning`; CI runs `--deny error`** | Adding the first recurrence row made `lint` exit 1, which exposed a policy that had never been tested against a long-lived finding. Every finding until then was a structural defect fixable by editing the library — a slug collision, a dangling citation — so "any actionable finding fails the run" was correct by accident. `UnheldRecurrence` is different in kind: it reports that a rule needs **promoting to a stronger control**, and the control may have to be built in another repository, so the finding legitimately stands for weeks. `lint`'s own module doc already says findings are "reported, never acted on — input to a human cut-list decision", and a human decision is not a CI gate. Blocking on it leaves `main` red until unrelated work lands elsewhere, and an always-red check is a muted check (`[R:xplat-fixtures]`) — the finding would be ignored precisely because it is always there. The threshold makes warnings non-fatal **without making them invisible**: every finding is still printed at every level, and the summary names the threshold so a green log explains itself. The default is unchanged, so no existing invocation became permissive by accident, and an `Error` (two rules colliding on one output file — the emitted tree genuinely wrong) still fails | *`continue-on-error: true` on the CI step* — the same effect with none of the meaning; a step that cannot fail is decoration, and nothing records *why* it cannot. *Demoting `UnheldRecurrence` to `Info`* — treats the symptom by lying about severity; the finding really is worth a look, which is what `Warning` means. *Leaving `lint` out of CI* — what the repository had, and it is `[R:wired-artifact]`: a check nothing invokes. *Blocking on warnings and clearing the finding by graduating the rule* — would require `status = graduated, to = "hook:gate-verdict-intact"`, which is **false for half the rule** (the hook holds the pipeline half; the edit half is prose-only) and would print that false claim into four instruction layers |
| 2026-09-06 | **A skill `description` is a bounded trigger built from rule *titles*, not an inventory of titles and error classes** | `description` is the only string Claude reads when deciding whether to load a skill, and it is capped at install time. The old inline format interpolated every rule's title *and* error class, so at 46 rules `skills/global/SKILL.md` carried **6924** characters against a 1024 cap and `domain-rust` **4322** — the two skills anyone would want were uninstallable, and would have been unusable matchers if they had installed. The error class is the wrong material twice over: it describes the *failure* in reviewer-facing prose ("Encoding a distinct state as a magic value ... that downstream logic must remember to special-case") where the title names the *practice* in a few words, and it is what made the field seven times too long. The bound is held by a type — `SkillDescription`, private field, whose only constructor truncates — so an over-cap description cannot be built rather than being a bug to catch (`[R:prefer-by-construction]`). The lead sentence is derived from `Home`, the same derivation `LoadSemantics` already makes for Cursor's globs, so the emitter still reads nothing the rule does not carry. **Why nothing caught it:** every unit test in this emitter builds a two-rule library, where the old format is comfortably short and stays short forever — the defect only exists at the size the real corpus reached, which is why the enforcing artefact is an assertion over the committed corpus rather than another generated case | *Truncating at the call site* — puts the cap where the next caller will forget it, which is the guard-after-the-fact shape this repository prefers to design out. *A hand-authored `description` per home carried in the library* — the best matcher text, and it breaks the invariant that makes every other guarantee here cheap to trust: emitters read only what the neutral rule carries, and a per-target prose field is the first vendor field in a neutral format. *Keeping error classes and raising the cap* — the cap is Anthropic's, not ours |
| 2026-09-06 | **`claude-pack/` is a second emission of the `claude` target, with its own CI verify step** | Exactly the `copilot-pack/` argument one target over: the folder has to be downloadable and installable into a repository that has no Rust toolchain and cannot run `relearn build`, and it must be **generated rather than transcribed**, so a stale or hand-edited copy is detectable by the same marker and hash as the root tree. It needs its own CI invocation because it is a second output root, outside every relearn-owned path, so the bare `verify` cannot see it — without the step a rule change would rebuild the root artefacts and leave the distributed copy silently stale, which is the distributed-copy case of `[R:doc-currency]`. All four home layers are included rather than filtering to `global` + `domain-rust` with `--home`: one build and one verify, no flags to keep in sync between the two, and the README says plainly which layers to take | *Filtering to the two portable layers* — needs two `--home` builds into one root and leaves a bare `verify` of the pack reporting the omitted skills as missing, so the gate would need flags that match the build exactly or go quietly wrong. *Not committing the pack* — what a downloader gets is then whatever the last person built, which is the untracked-generated-artefact drift already recorded against `~/.claude/rules/domain-rust.md` |
| 2026-09-06 | **`origin: Mined \| Codified` is a MANDATORY field, and all 46 rule files were migrated** | It is the counter-metric to recurrence, and P5 says a metric never ships without one. Recurrence is only ever incremented by someone willing to write down that their own rule failed, so it falls through under-reporting exactly as easily as through prevention; the counter is the corpus's *inert* fraction — never recurred **and** never mined from a real failure — because the null result says those are the rules that change nothing. Mandatory rather than `#[serde(default)]` because a defaulted origin classifies unlabelled rules silently, which is precisely the under-reporting the counter exists to detect: a metric computed from guesses is worse than none, since it looks like a measurement. This project's charter already makes provenance mandatory, and origin is provenance. **The classification is the author's, not the migrator's**: 19 rules were marked `codified` because their own `incident` text says "Codification-dated"; the other 27 are `mined`. The tempting second signal — the word "ported", present in 32 rules — was measured and rejected: it describes migration *between homes*, not origin, and using it would have misclassified about thirteen rules | *`#[serde(default)]` with `Mined`* — no migration, and it silently asserts that every unlabelled rule came from a real failure, inflating the honest half of the ratio. *A third `Undeclared` variant* — a sentinel by another name, and it would let the unknown fraction masquerade as a measured one. *`Codified(source)` carrying its document* — considered and deferred: "codified without a source" is not an illegal state, so a payload makes nothing unrepresentable, and the source is already in the `incident` prose |
| 2026-09-06 | **`Status::Graduated` carries a `date`, and a recurrence after it is an `Error`** | Without a date nothing could ask the only question that matters about a graduated rule — *did it recur since it graduated?* A recurrence **after** the named stronger control was in place means the emitted layer's `> Also enforced by {to}` line is false in every one of the five formats: a lying artefact in a live artefact (`[R:repair-the-lying-artefact]`), which is why this is the one recurrence finding that is an `Error` and fails CI. A recurrence **before** the graduation is explicitly not a finding — it is very often the incident that prompted the graduation, and flagging it would punish exactly the response the library wants. The boundary is `>`, not `>=`, and is pinned: a recurrence *on* the graduation date is not after it, and an off-by-one there manufactures an `Error` out of the day the control landed. The three graduated rules were dated from git — the first commit of each hook, 2026-07-21 — rather than from a plausible guess | *Leaving `Graduated` dateless and comparing against the rule's `created`* — wrong question: `created` is when the prose was written, not when the stronger control landed, and the gap between them is exactly the interval this finding is about. *Folding it into `UnheldRecurrence`* — merges two findings with different severities and different remedies into one accurate for neither, and would say a graduated rule is "held by prose alone", which is false |
| 2026-09-12 | **The dependency gate is a test, and it reads a marker in the Decision cell** | `[R:price-every-dependency]` needs a mechanical half, and `tests/dependencies.rs` is it: every dependency in every manifest must be named by a decisions-log row whose **Decision** cell opens with `Dependency:` or `Dependencies:`, the crate named in backticks. A test rather than a script because a test is wired by construction — `cargo test` already runs in CI on Linux and Windows, so the gate cannot arrive disarmed the way `scripts/no-banned-names.sh` can, and it needs no new CI step to remember. The marker must *open* the cell rather than appear anywhere in the row, so that a row discussing the convention — this one — cannot price a crate by naming it in passing: `[R:detector-excludes-own-definitions]`, which this check would otherwise have failed on its first day. Nine dependencies predating the rule are exempted by name in `GRANDFATHERED`, a list two further tests force to shrink: an exemption that is now priced, or whose crate has left the manifest, fails. A fourth test asserts the log section is still where the gate looks, because renaming it would empty the priced set and leave a gate that passes while measuring nothing. Probed in eight directions before being trusted. | *A bash script beside `no-banned-names.sh`* — the same shape as a control this repository already records as configuration-dependent, and it would need a CI step nobody re-reads. *Substring matching on the crate name anywhere in the log* — would have read `serde` (an attribute mention), `sha2` (inside a rejected alternative) and `toml` as priced on day one, reporting a gate that measures nothing. *Retrofitting the nine existing entries instead of exempting them* — means reconstructing rejected lists nobody remembers, which `[R:decisions-log-records-rejected-alternatives]` warns produces the flattering version. *Graduating the rule to this test* — refused: `Status::Graduated { to }` is single-destination, and the emitted "Also enforced by" line would be false for the refused half, which no gate can ever hold |
| 2026-09-13 | **Audience is a separate optional field (`applies_to`), never a nested home** | `Home` was answering two questions with different cardinality. *Who owns and maintains this rule* has exactly one answer and is the P2 invariant the tool exists to enforce; *who should load it* has several. The case that forced the split is low-latency knowledge — NUMA residence, cache-line behaviour, why a busy-spin core must be exclusive — which Rust and Java engineers both need: homed in `domain-rust` it is noise for one language and invisible to the other, homed in `domain-low-latency` it is invisible to both because nobody's Rust build asks for that home, and written twice it is two homes drifting, in the tool built to prevent two homes. `ScopeTag` is a witness newtype parsed at the perimeter (lowercase kebab, bounded, trimmed), `applies_to` is `#[serde(default)]` and serialised only when non-empty, and **scope never reaches an emitted path or body** — `Home` alone decides where a rule lands, scope decides only whether it is included, pinned by `scope_never_reaches_an_emitted_path_or_body`. The emission default is the load-bearing part and is held in `Rule::serves` rather than in the CLI, so no caller can reimplement it differently: an empty audience narrows nothing, and **an unscoped rule serves every audience**, so adding `applies_to` to one rule can never remove a different rule from an existing build. An audience no rule declares is an error for `build`/`verify` and an empty listing for `list` — a distinction sharper than the `--home` one, because an unknown home emits nothing (which looks wrong) while an unknown scope emits a tree quietly missing every scoped rule (which looks like success) | ***Nesting the hierarchy*** (`rust/low-latency`, `java/low-latency`) — the instinct, and the reason this row exists: a tree expresses one axis and forks the other. What varies by language is the implementation; what varies by discipline is the principle, so nesting puts the shared principle in two branches where it drifts from the first commit — the exact P2 violation `relearn` exists to prevent. *Deriving the audience from `Home`* — no new field, and it cannot express a rule two languages need without inventing a home neither declares. *A second home field* — two homes for one rule is unrepresentable by design, and the whole point is that audience is not ownership. *Scope affecting the emitted path* (a `rust-low-latency.md`) — would make the path a function of two independent axes and multiply the emitted tree by the scope set; it also breaks the pairing between `build --home X` and `verify --home X`. *Defaulting an unscoped rule to "no audience"* — inverts the safety property: every rule written before the field existed would vanish from a scoped build, which is a silently dropped correction, the failure at the top of `CLAUDE.md`. *A controlled scope vocabulary now* — real problem, different problem: recorded in `TODO.md` rather than designed here |
| 2026-09-13 | **An install names its audience on the command line; a config file is refused, not deferred** | `docs/federated-relearn.md` §2 said an install declares its scopes *in config*. That is a per-machine file making the same corpus compile differently in two places, which §1's invariant 3 forbids and `tests/solo_mode.rs` fails on — the contradiction arrived when §1 was added after §2 and nobody re-read §2 (`[R:revision-integrity]`), and by the time A1 shipped `--scope` the sentence described a mechanism that does not exist while contradicting one that does. The flag is also the better mechanism on its own merits: an invocation is visible in the shell history, in the CI file and in the reviewed diff, so two machines running the same command emit the same tree and a disagreement is readable rather than latent in someone's home directory. **The condition that would reopen it**, stated so the next person does not have to re-derive it: an install that genuinely needs the same scope set on every invocation, at which point the argument must be made against invariant 3 explicitly and `tests/solo_mode.rs` changes in the same commit with the reason written down. A config that arrives quietly is the thousand-conveniences erosion the test exists to stop | *A config file now* — the original text, and the one thing the solo-mode guarantee names as its own failure mode; it also makes `build` and its paired `verify` depend on a file neither command mentions, so a stale config silently changes what the gate is checking. *A config file supported but optional* — worse than either: the flag and the file can disagree, and the precedence rule between them becomes a thing to remember. *An environment variable* — the same ambient state with less visibility than a file, and `solo_mode.rs` catches a `dirs::` call but not an `env::var` someone adds "temporarily" |
| 2026-09-13 | **The install pseudonym lives inside the cloned aggregate repository, never on the machine** | A recurrence report must *replace* an install's previous report rather than add a second one, so the pseudonym has to survive between runs — and anything surviving between runs on the machine is exactly the per-machine state invariant 3 forbids. Putting it in the clone resolves both: `reports/<install-id>.toml` **is** the id, the clone is a path the human passes on the command line, `report` reads the id from the file already there and mints one only when there is none, and `tests/solo_mode.rs` stays green with no exemption and no new carve-out. It also matches a mental model people already have — delete the clone, the pseudonym is gone — and it keeps the id in the same place as the thing it identifies. The cost is recorded rather than hidden: the pseudonym is exactly as private as the clone, so a shared or backed-up checkout shares it | *A dotfile under the home directory* — the obvious implementation and the one that breaks the invariant the whole solo-mode gate exists to hold. *Passing the pseudonym on the command line every time* — keeps the human in control, and a mistyped id silently forks one install's history into two, inflating every count it appears in with no way for the aggregate to detect it; a privacy mechanism that fails by quietly corrupting the data it protects is worse than one that fails loudly. *Deriving it from a machine fingerprint* — stable without storage, and it is precisely the identifier the anonymity design forbids: derivable means re-derivable by anyone holding the same inputs. *No pseudonym at all, one report file per submission* — turns replacement into accumulation, so an install's diligence over time reads as many installs, which is the confound §8 already warns about, manufactured by the format |
| 2026-09-13 | **`Home::Org` federates through an exhaustive match, never a filter** | The design's load-bearing promise is that an organisation's rules can never leave the machine. A filter in the contribution path (`if !matches!(home, Org)`) satisfies it exactly once: it compiles perfectly while a second publishing path — a report, an export, a future command — publishes everything, and nothing objects. `Home::federation` is a match with no catch-all arm, so **adding a home variant is a compile error at every site that has to decide about it**, and `Rule::is_publishable` reads that one answer so no second predicate can drift from it. `Federation` is a two-valued enum rather than a `bool` because this is the one answer in the codebase where reading it backwards publishes something private, and a bare `true` at a call site says nothing about which way round it runs. The compile-fail pin asserts the mechanism by its error (`E0004`, naming `Org`), and a source-level test asserts the absence of a wildcard — the half no compiler can object to, probed in both directions before being trusted. `Project` is `Withheld` alongside `Org`, for a different reason that happens to share an answer: the home carries a filesystem path, which is a private identifier, and a stranger's project home is meaningless upstream. Establishing the exclusion **before** `contribute` and `report` exist is the order that matters: an exclusion added afterwards is one somebody has to remember to apply | *A filter in the publishing path* — the obvious implementation, correct until the second path exists, and it fails silently when it fails. *A `#[non_exhaustive]` `Home` with a catch-all* — makes every match compile forever, which is precisely the failure: a variant added later inherits whichever default the wildcard names, and nobody is asked. *A `bool` on `Rule` (`can_publish`)* — a second place for the answer to live and therefore a second place to drift, and it moves the decision off the type that owns it. *Deferring the exclusion until B2, when something can actually publish* — the phase that adds publishing is the phase with every reason to skip a guard it does not yet need |
| 2026-09-13 | **A mandate carries its approval as the variant's payload; recurrence statistics hold mandates out** | Control-framework requirements are a different species from corrections: no incident, never mined, provenance is the sign-off rather than the failure. `Origin::Mandated(Approval)` makes **both** illegal states unconstructible at once — a mandate with no signer, and an approval attached to a mined or codified rule — where an `Option<Approval>` field on `Rule` would make both representable and turn the invariant into a check every construction site has to remember. `Origin::parse` takes the spelling and the optional table together, so the correspondence is settled at one perimeter that can see both halves, and each mismatch is a named error rather than a silent drop: an ignored approval reads, to the next person, as a rule that was signed off when it was not. Mandates are then excluded from **both** counter-metric numbers through one predicate (`Rule::counts_toward_recurrence_statistics`, read twice by `lint::tally` rather than re-derived): a mandate cannot be recurrence evidence, and it is not *inert* either, because inert means "authored and never fired" — a judgement about something that was meant to be evidence. `Tally` reports the hold-out and the evidential denominator so the exclusion is visible, and the CLI prints them only when a mandate exists, because a permanently-zero column is one readers learn to skip | *`approval: Option<Approval>` on `Rule`* — the shape the prompt's wording suggests, and it represents both illegal states; the invariant then lives in a validation somebody must re-run at every construction site. *A plain `Mandated` variant with the approval in `incident` prose* — provenance that no code can read, so no gate can ever check it, and the `incident` field means something different for that one origin. *Counting mandates as inert* — the default if nothing is done, and it inflates the counter-metric with rules that were never supposed to be evidence, swamping the only number that says whether prose is holding. *Excluding mandates silently, with no count* — the correction to the above, and it hides its own denominator: a reader cannot tell a library of five rules from one of five hundred with 495 mandates |
| 2026-09-13 | **The cached-rule refusal is a witness type at the write boundary, not a check inside the writer** | Editing a cache in place is a *silent* fork: the edit succeeds, P2 is gone, and nothing records that the copy and its source have diverged. A check inside `write_rule` would be satisfied exactly once — the second rule-file write path, added later by someone who never read this row, would compile and fork a cache with nothing to object. `fsio::write_rule` therefore takes an `EditableRule`, a witness whose only constructor refuses `Authority::Cached`, so a future write path **cannot be called** without asking the same question; a compile-fail pin asserts that passing a bare `&Rule` does not compile, by its error rather than by merely failing. The same shape as `Library<Validated>` gating emission and the same argument as `[R:generate-guards-unversioned]`: a marker the code recognises, never discipline at each call site. `Authority` has **three** variants rather than two because "adopted" is the state a two-state model loses — a cache that became plain `Local` would be indistinguishable from a rule authored here, which is the silent fork arriving through the very command that exists to make forking deliberate. `Version` is a monotonic `u32` because the total order is the whole requirement: two caches of one rule must be comparable, always, or "is this stale?" has no answer for some pairs. `adopt` takes the date as an argument rather than reading a clock, and `tests/solo_mode.rs` now forbids clock reads so that decision cannot quietly erode | *A check inside `write_rule`* — correct today, absent from the next write path, and it fails silently when it fails. *A `bool` field (`is_cached`) consulted at each call site* — the same fragility with more places to forget. *Two `Authority` variants, adoption returning to `Local`* — loses the fork's provenance, so an adopted rule and an original become indistinguishable. *A content hash as the version* — tells you two copies differ and never which is newer, which is not the question a staleness check asks. *A timestamp as the version* — two publishes in one day collide, and clock skew across machines means it is not reliably ordered at all. *Reading `SystemTime::now()` for the adoption date* — the obvious implementation: ambient state, unreproducible output, and untestable without freezing time |
| 2026-09-13 | **A contribution is a projection that never holds the raw incident, and the scrub stays on the contributor** | `incident` is a verbatim quotation from a private working session and cannot travel; so can no `[[recurrence]]` incident, and there are more of those. A filter that strips them is code somebody can edit, reorder or duplicate — `Contribution` instead **borrows only the publishable fields and never borrows those at all**, so no renderer can emit them, forget to strip them, or strip them wrongly. What travels is `published_incident`, a **separate authored field of its own type**, so the compiler refuses to put a raw incident where a published one belongs. Nothing derives one from the other, and nothing ever should: an automatic scrubber leaks what it did not recognise, destroys context it did not understand, and — worst — stops people reading the output because something appears to be handling it. The four refusals (`HomeIsWithheld`, `NoPublishedIncident`, `IsACache`, `IsMandated`) are checked once, at the only place a `Contribution` can be constructed. `HomeIsWithheld` is A2's exclusion finally consumed; `IsMandated` is new and forced by the projection — a mandate's provenance is an approver by name, and a mandate stripped of its approval would claim a sign-off it no longer records. `contribute` prints and writes **nothing** without `--confirm`, because the only real control on what a published incident says is a person reading it | *Stripping the incident in the renderer* — one edit away from not stripping it, and the mistake is invisible until it is public. *Deriving the published incident from the raw one* — the "helpful scrubber" the design names as a failure mode twice over. *Reusing `Incident` for both* — makes the swap a call-site mistake instead of a compile error. *Carrying recurrence dates without their text* — a count that says nothing about the rule and a shape inviting the incident back later; anonymous counts are the report flow's job. *An interactive stdin prompt for confirmation* — needs a TTY, cannot be tested, and trains people to press y. *Stripping the approval and publishing the mandate anyway* — publishes a rule claiming a sign-off that is no longer recorded |
| 2026-09-13 | **The banned-terms matcher is reimplemented in Rust; the list format is the contract** | §12.2 requires the contribution path to run the matcher over the published incident before writing, and names the split: the matcher is reusable, its file-walking is not, because the target is one string rather than a tree. Sharing the implementation means spawning `scripts/no-banned-names.sh`, which `tests/solo_mode.rs` forbids — so there are two implementations of one matcher and the **term-list format is the contract between them**, documented in both. Both of the script's load-bearing properties carry over: a finding reports a location and a length and **never the match** (`[R:report-the-hit-not-the-match]` — printing it moves the exposure into a terminal or a CI log rather than closing it), and **disarmed is never clean**. On the second, the Rust side is stronger than the script: `--terms` is a required argument and `TermList::parse` is the only constructor, so "ran with no list" is unconstructible rather than an exit code to remember. The list is a path given on the command line, not looked up in a home directory, because the tool consults no ambient state | *Spawning the script* — forbidden by the solo-mode gate, and it would make a contribution depend on bash and perl. *Committing the term list so both sides share one file* — this repository is public and one protected name normalises to four characters; publishing the salt publishes the name. *Making `--terms` optional with a warning* — the disarmed-gate failure the script's exit 2 exists to prevent, reintroduced for convenience. *Reading `$BANNED_TERMS_FILE` or `~/.claude/usage/`* — ambient state, which `solo_mode` forbids and which makes the same command behave differently on two machines. *Reporting every hit rather than the first* — a list of locations is a longer description of the same secret |
| 2026-09-13 | **The report's absences are types, and the pseudonym is the filename in the clone** | A recurrence report is the one flow that must be anonymous — recurrence is somebody writing down that a rule of theirs failed, and nobody does that with their name on it. So the field list is enforced by `Observation` having exactly five fields rather than by a renderer that strips the rest, and the month-level date is enforced by `Month` having no day field rather than by formatting carefully: `Month::of(Date)` is the only route from a date into a report and it drops the day by construction. Who is reported is four questions asked in one place, and two of them are earlier phases finally consuming themselves — A2's `Federation` (an org rule never appears in a report, as it can never be contributed) and B1's `Authority` (**only an upstream tag has a shared identity**; a local-only tag is a private name, and publishing one is the leak). `Control` is sealed at five values so `gate:internal-payments-lint` publishes `gate` and nothing more. **Phase 0.2's contradiction is resolved here in code**: the pseudonym must survive between runs for a report to be *replaced* rather than duplicated, and anything surviving on the machine is the per-machine state invariant 3 forbids — so it lives in the cloned aggregate repository, where `reports/<install-id>.toml` **is** the id, recovered from the file stem. `--install` is required only on first use; after that the clone supplies it, a conflicting one is refused, and a clone holding several reports is an error | *A dotfile under the home directory* — the obvious implementation and the one `tests/solo_mode.rs` exists to catch. *An environment variable* — the same ambient state with less visibility; `solo_mode`'s `env::var` marker catches it, which is why that marker predates this phase. *Typing `--install` every run* — a typo forks one install's history into two and inflates every count it appears in, with no way for the aggregate to detect it; required once, it is harmless. *Minting a random id* — needs an entropy source, which is a dependency and ambient state both. *Stripping fields in a renderer instead of a five-field type* — one edit away from not stripping them, and the mistake is only visible once published. *Formatting dates to the month from a full `Date`* — a formatting rule anybody can get wrong later, where a day-free type cannot be. *Choosing one kind when a destination names two* — three of this corpus's eight graduations name two, and picking one would invent a fact; the field is omitted instead |
| 2026-09-13 | **The aggregate is a recompute over a clone, with the floor and the confounds built into the document rather than into a caller** | §9's argument is that a repository beats a service: publication is deliberate by construction because it is a pull request, every byte that ever crossed is auditable in public history, review is free because it is code review, and a failure is an empty repository rather than an outage. So the infrastructure is one command a scheduled job runs over `reports/*.toml` in a clone, reading nothing else — no fetch, which `tests/solo_mode.rs` keeps true against the "for convenience" version the programme predicts. Three things are built into the document rather than left to whoever renders it. **The k-floor** is applied where the counts are and re-exported from `report`, so the producer and the aggregate read one constant rather than two policies that drift; below it a rule does not appear **at all**, because publishing a tag while withholding its count points at the same person. The floor counts **distinct installs**, since one install repeating a report is the obvious way to fake a population. **The suppressed count** publishes while the suppressed tags do not, so an aggregate cannot read as complete. **Both confounds** are a `const` written unconditionally, including in an empty aggregate — the case where dropping them is most tempting and most misleading — because the way they get dropped is by being droppable. And **an aggregate is never authoritative**: a source-level scan asserts no emitter or build-path file depends on it, since nothing else can see a build that has quietly learned to consult a downloaded file | *A service with an API* — needs hosting, accounts, a database and a privacy policy, and it makes publication a silent POST rather than a reviewed pull request. *A fetch subcommand* — the exact failure the solo-mode gate exists to prevent, and it will be proposed as an improvement. *Applying the floor in the renderer* — leaves the suppressed rows in the value, one edit away from being printed. *Publishing the tag but withholding the count below the floor* — the tag identifies the reporter as well as the count does. *Counting reports rather than distinct installs* — a floor one install can clear alone is not a floor. *Omitting suppressed rules silently* — the document then reads as complete, and a reader cannot tell a small corpus from a heavily suppressed one. *Making the confounds a `--verbose` flag* — a headline figure without them is the overclaim this project exists to prevent, and a flag is how it becomes one. *Skipping reports that fail to parse* — an aggregate that ignores what it cannot read publishes a count quietly missing whoever wrote it |
| 2026-09-13 | **The poke lives in `lint`, one trigger is reactive and uncapped, and a revision on `Authority::Local` is what makes staleness answerable** | §6's argument is that the reactive trigger — *a rule fired here, and upstream already has one for that class* — is what makes a corpus a collective memory rather than a mailing list: it arrives at the moment somebody has demonstrated they needed it. Everything else is broadcast, and broadcast is how a notification channel teaches people to ignore it; Paper 3 §9 documents that failure already on exposed rows, so broadcast is **capped per run** and two of the three are off until named. The defaults are one exhaustive match (`Trigger::on_by_default`) rather than behaviour spread over call sites, and the cap reaches broadcast only because `Reach` is a property of the trigger rather than a `bool` where the cap is applied. It lives in `lint` because `lint` already runs on every change in CI — an invocation actually on the path, which is the sharpest criticism of an enforced-by column that records enforcement and never invocation — and a separate `relearn news` is a command nobody runs. A poke carries **no severity**: it is printed after the exit code is decided and cannot change it, because a signal from strangers that can fail CI has made federation required, which invariant 3 forbids. The stale-cache trigger forced the one model change: an upstream rule had no way to say which revision it was, so `Authority::Local` now carries `Option<Version>` — **the same field a cache carries, answering the same question**, which is why it went inside the authority rather than beside it. `None` is a real state, not a zero: absent, no cache can be told it is stale and the poke says nothing. A copy's provenance under `kind = "local"` is now a parse error rather than a silently dropped field, and the aggregate's generation month moved from the value to `to_toml`, because it describes the *document* and the poke consumes rows without publishing anything — it needed no date, and inventing one would have been a date this tool did not receive | *A `SessionStart` hook installed by this phase* — anything under `~/.claude` edits the layer loaded into every session on the machine, and no gate in this repository could see it; the snippet is written into `docs/session-start-poke.md` instead. *Broadcast triggers on by default* — easier to demo, and within a month the poke is a notification people dismiss, including the one that mattered. *Capping all pokes rather than broadcast only* — withholds the reader's own news to make room for a stranger's. *The cap as a constant in the code* — §6 asks for a number the operator sets; there is no config file to hold one and there must not be, so it is a flag with a published default. *A `--poke` flag that adds to the defaults* — then no single flag says what will fire. *Reading the committed `aggregate.toml`* — nothing verifies that a published aggregate matches the reports beside it, and it would need a second parser to drift from the writer. *A tuned integer for "high recurrence"* — a threshold nobody can check; the published top bucket is a boundary a reader can look up. *A top-level `version` field on `Rule`* — a cached file would then carry its revision twice, once as the revision it is and once as the revision it holds. *A fourth `Authority` variant for a published home* — splits "this install owns it" in two, so every `matches!(a, Local)` silently answers false for the new one. *A content hash instead of a revision* — tells you two copies differ and never which is newer, which `Version` was introduced to answer. *Treating an absent upstream revision as zero* — the sentinel that would tell every cache it is stale forever. *A poke that fires during `build`* — `lint` reports and `build` emits; mixing them is a build that talks |
| 2026-09-13 | **A hand-written count about a generated file is checked in both directions, and each check owns its own denominator** | The pack READMEs state *"52 rules, 746 lines"* and five per-layer counts about files `relearn build` writes. Nothing read them, which the federation programme named twice as the standing exposure and which is `[R:guarantee-needs-a-reader]` in the repository whose own corpus carries that rule. Two directions, because the obvious one misses what actually drifts: every **claim** must match its file, **and** every file must be claimed — the second catches a sixth home layer arriving, `build` writing its `SKILL.md`, `verify` staying green because the pack matches the rules, and the README still listing five, where every remaining claim is still correct and the inventory is a lie by omission. It does **not** check that a pack matches the rules: `verify --out <pack>` holds that, CI runs it as a second output root, and the two compose to README ↔ pack ↔ rules without either needing to know how the other measures (`[R:measure-the-claim-not-a-subset]`). Scope is stated in the test's own header because that rule is exactly what goes wrong here: a *rule* is a `## ` heading ending in its `[R:tag]`, a *line* is `str::lines()`, which strips a trailing `\r` so a CRLF checkout counts the same. A README stating no counts at all fails, because a parser that has stopped recognising the inventory would otherwise pass silently | *A script under `scripts/`* — CI would have to remember to call it, and the two gates that already exist as tests are there precisely because a test is wired by construction. *Parsing the ASCII tree to resolve a path* — the tree is decoration; a redrawn box character would break the check while the numbers it guards stayed wrong, so files are resolved by suffix match and an ambiguous token is an error rather than a first match. *Checking claims only* — leaves the omission direction open, which is the one that actually happens. *Deriving the counts from the rule library instead of from the pack files* — measures a different denominator from the one the sentence claims, and would pass while a stale pack sat on disk. *Regenerating the README* — the prose around the numbers is hand-authored install guidance, and a generator would either destroy it or need a template nobody would maintain |
| 2026-09-13 | **An upstream retirement is the fifth poke, not the lint Warning §12.6 specifies** | §12.6 was written before the poke existed and gives `CachedRuleRetiredUpstream` as a lint `Warning`. A `Warning` can only be reached with `--upstream`, and `--deny warning` is the default — so as specified it is federation failing a run, which the single-install guarantee forbids and which the poke was shaped a day earlier to make impossible. Same input, same reach, same inability to touch a verdict: it belongs beside `cache-behind` as a broadcast poke, **on by default**, because it cannot fire at all against an install that caches nothing and because §12.6 wanted it prominent. What survives the change unaltered is the part that matters: an upstream attic **only warns**. Deleting an instruction a team relies on because a stranger retired it is a correction lost with no reader — P1 — and the local install may hold evidence the upstream author does not; `adopt` on an upstream retirement is then the most valuable signal the federation can produce, so the message names it first of the three resolutions. The suppression is unreachable rather than declined, because only a local `Status` reaches `Status::emittability`, and the test asserts that over the real emitters rather than over the argument. The trigger reads **only** an attic — a graduated rule is still emitted, annotated with the control that also holds it, so only tag-level death qualifies, which is why §12.6 needed no new `Status` variant | *Transcribing §12.6 as a lint Warning* — the design is the source, but a sentence written before an invariant existed is not a licence to break the invariant; the deviation is recorded in §12.6 itself rather than left for a reader to discover in the code. *An `Info` finding instead* — cannot fail a run either, but it sits in a list `--deny` grades, so the next person to raise a threshold would make federation fatal without noticing. *Off by default* — it fires only about copies you already hold, so it is bounded by your own caches and silent for everyone else; defaulting it off would mean nobody ever sees the retirement they are ignoring. *Suppressing emission on an upstream attic* — the P1 failure this whole rule exists to name. *Treating a graduated upstream rule as retired* — a graduated rule is live and annotated; conflating the two would withdraw guidance that was strengthened, not withdrawn |
| 2026-09-13 | **The ledger's `Enforced by:` claims are read, and the check's own reach is measured rather than assumed** | `FEATURES.md` makes seventy claims about what holds each guarantee, and nothing read them: a test renamed in a refactor leaves its row naming something gone, which is `[R:guarantee-needs-a-reader]` and `[R:wired-artifact]` in the file whose entire job is to prevent exactly that. Two directions again, each owning its denominator — every path-qualified citation must resolve to a file that defines that function, and every row must name something in backticks or say `NOTHING YET`. Only the **path-qualified** form is resolved, and the scope is stated in the test rather than left to a reader to infer: a bare `module::item` citation would have to be searched for, and the same shape covers `env::var` and `ExitCode::FAILURE`, so the checker would carry a growing exclusion list of standard-library names until somebody muted it. Line comments are stripped before the lookup, because a test named only in a comment must not satisfy a claim that it exists. Both directions carry a floor well below the real count, so a parser that stops recognising the ledger fails instead of passing, and adding rows never requires editing the check | *Resolving bare `module::item` citations too* — the exclusion list of foreign names is the muting mechanism, arriving one entry at a time. *Asking only whether a backtick appears after the marker* — the first version, and vacuous: every row satisfies it by accident, because the rows are paragraphs. Replaced by a sixty-character window derived from measuring the ledger (fifty-eight rows open with their citation; the rest reach it within thirty), so the bound is a measurement rather than a taste. *Regenerating `FEATURES.md`* — the rows are argument, not data; a generator would either destroy the reasoning or need a template nobody maintains. *Allowing `process-control` as a third category beside artefact and gap* — an exclusion list of one, and the ledger's own header had already classified that row as exposed; the row was corrected to agree with the header instead |
| 2026-09-13 | **`pull` is the receiving half, and it takes a second witness rather than a flag on the first** | This install could publish a rule and could not receive one: `Authority::Cached`, `adopt` and three poke triggers all read caches, and nothing created one, so the whole federation was inert. `pull` is the mirror of `contribute` — a rule arrives because somebody asked for it by tag, from a directory both installs can see, which is §10's *no automatic sync, in either direction* and *no silent adoption*. The shape decision is the witness. `fsio::write_rule` takes an `EditableRule` whose constructor **refuses a cache**, which is exactly what a pull must write; a `bool` to skip that would make "edit a cache in place" reachable by passing `true`, and that state is the one B1 spent a phase making unconstructible. So there are two write paths taking two witnesses, and neither can be minted for the other's subject. `PulledRule` **owns** its rule rather than borrowing one, because the cache does not exist yet when the witness is minted — the constructor is therefore the single place a cache is ever built, with no second site to keep in step. Its four refusals are asked there, once: a withheld home never arrives for the same reason it never leaves, an unnumbered upstream rule is refused rather than cached (a copy of it could never be told it is stale), and pulling over a local rule or a deliberate fork would destroy hand-authored source or a recorded fork respectively. Re-pulling over a cache is the update path. The cache keeps the home it arrived with — `Home` says which layer, `Authority` says who maintains it, and the independence is what lets a pulled rule compile like a local one. `contribute` gained a required `--version` in the same change, because a publication with no revision produces a cache whose staleness signal is dead on arrival | *A `force` or `as_cache` flag on `write_rule`* — reopens the exact state the cached-rule witness exists to close, and does it with a parameter somebody will pass. *`PulledRule` borrowing the upstream rule* — the cache is a different rule from the document it came from (different authority, no recurrences, no published incident), so a borrow would push its construction out to the caller and there would be two places a cache is built. *Copying upstream's recurrences into the cache* — fabricates local evidence for a rule this install has never seen fire. *Reading the version from the contributing rule's own authority* — a fork's records what it was forked at, so publishing would reuse a revision that already exists upstream. *Defaulting `--version`* — a silently defaulted revision is a number nobody chose, and monotonicity is the whole value of it. *Fetching over a network* — `tests/solo_mode.rs` forbids it, and a shared directory needs nothing the tool does not already have |
| 2026-09-13 | **A republication must supersede, enforced by a witness rather than a check — and all three rule-file writers share one guard** | A cache records the revision it holds and `cache-behind` decides staleness by comparing numbers, so republishing at or below what is already published makes every existing cache read as newer than upstream **and nothing notices**, because the comparison is exactly what has been corrupted. That is a silent, self-concealing failure, so it is made unconstructible: `SupersedingVersion` is minted from the proposed revision and the destination's current one, and `Contribution::to_document` takes only that type. A backwards republication is therefore not a call-site mistake but a document that cannot be built. The error carries both numbers, because a contributor told only "refused" has to go and look up the one the tool has just read. Building it exposed the larger defect beside it: `contribute` had written with a **bare `fs::write` since B2**, no clobber guard at all, while `write_rule` and `write_cache` both refused to overwrite a file that was not their rule. It survived because it was the only rule-file writer with no sibling to be compared against — the moment `write_cache` landed, two guarded and one did not. All three now delegate to one private `guarded_write`, so the refusal is inherited rather than reimplemented, and the guard is about the **path** (a file that does not parse, or parses to another tag, is somebody else's) where the witnesses are about **authority** | *Checking the revision in `contribute` before calling the renderer* — correct once, and absent from the next caller; the same argument as `EditableRule`, and the reason this repository keeps choosing witnesses over checks. *Deriving the revision from the destination — publish `published + 1` automatically* — hides the number the contributor is asserting, and two people publishing concurrently would both compute the same next value. *Comparing content and refusing a no-op republication* — the revision means "the sixth thing I published under this tag", not "the sixth distinct one"; a content check turns a wasted command into an error. *Leaving `contribute` unguarded because the path is derived from the tag* — it is, and the file at that path can still be a different rule, which is precisely what the probe showed. *Three separate guards* — the shape that produced the omission in the first place |
| 2026-09-13 | **A bulk pull is a plan first, an audience bounds it, and a prune reaches only caches** | `--all` had to answer three questions without becoming sync. **It is a report before it is an action**: run without `--confirm` it is the status check to run before starting work, and it names every rule in both corpora exactly once — including, and especially, the ones it will not touch and why. The half a bulk command usually hides is what it did *not* do, and a reader given only the actions has to diff two libraries by hand. **An audience bounds what it takes**, through the same `Rule::serves` every other narrowing uses, because §10's answer to a corpus too large to compile is scope rather than retrieval; an unscoped upstream rule is taken regardless, which is A1's safety default one level up. **And a prune removes only caches.** That is the whole safety argument and it rests on one property: a cache is *regenerable*, so dropping one loses nothing a later pull cannot restore, while a rule this install owns and a fork it took are source that nothing regenerates. `DroppableCache` is the third witness and refuses both, so `remove_cache` — the only path that deletes a rule file — cannot be reached with source. Dropping is opt-in, and an unwanted cache is still reported under the default, so the status check shows what a prune would take without taking it. This is §12.6's third resolution, *drop*, reached deliberately: an upstream attic still only warns, and what changed is that the warning now has a command to act on rather than only a sentence to read | *A sync that reconciles on a schedule* — §10 forbids automatic sync in either direction, and the value of a pull is that somebody asked. *Reporting only the actions* — the omission is the thing a bulk command hides, and it hides it silently. *Pruning by default* — the one operation that deletes a file, applied by whoever ran the ordinary command. *A `bool` for prune* — `Prune::Keep`/`Prune::Drop` reads at the call site; `true` does not. *Letting a prune remove a stale fork or an unused local rule* — both are source; the witness makes the question unaskable rather than answering it carefully. *Deciding the attic case from the upstream side as well as the held side* — the first version did, and reported one rule as current **and** unwanted in the same run |
| 2026-09-13 | **A contribution warns about citations the destination lacks; the publishable unit is a rule plus its closure** | Found by the first real subscriber rather than by a fixture: three rules were published, and the subscriber's **first** `lint` failed on two dangling references, because the citation closure of those three was five. A fixture publishes what it was written to publish; a real subset of a corpus whose rules cite each other does not. `contribute` already reads its destination to answer whether a revision supersedes, so it reads it for this at no extra cost. **A warning rather than a refusal**, and the two cases it distinguishes are why: a tag not published *yet* resolves when somebody publishes it, while a tag whose home never leaves a machine never resolves at all — and refusing the second would make every rule citing a project-homed one permanently unpublishable. The citation scan is `lint::cited_tags` made public rather than reimplemented, because it carries a decision a second copy would lose: it reads the **body only**, since provenance legitimately names retired, renamed and foreign tags, and scanning it once made the linter flag its own history | *Refusing to publish a rule with dangling citations* — makes any rule citing a withheld one unpublishable forever, which is a large class. *Offering to publish the closure* — cannot work: a closure member with no `published_incident` cannot be published either, so the offer would fail halfway and leave a partial publication. *Silence, leaving it to the subscriber's lint* — that is where it was found, and it fails the newcomer's first command rather than the publisher's. *A second citation scanner in `contribute`* — the body-only decision lives in one place and is the kind of thing a copy quietly loses |
| 2026-09-14 | **A scope vocabulary with no curator, and an edit-distance floor the design did not state** | §12.5's answer to scope drift is neither free strings nor a controlled list: the corpus *is* the vocabulary, and the near-misses a curator would have caught are detected instead. `ScopeTag` refuses the malformed spellings at parse time, so what survives is the well-formed slip — silent, because nothing fails and the rule merely serves an audience nobody asks for. The finding extends `OverlappingScope`'s shape rather than adding a parallel mechanism, as the design asks. **The one deviation is the distance rule.** Taken literally, edit distance 2 flags `rust` against `ruby` — two languages — and flags any two two-letter scopes against each other, because two edits is the whole of a two-letter word; both are false positives in the very vocabulary §12.5 uses as its examples, and a heuristic that fires on a legitimate pair is how a check gets muted. Distance is therefore read relative to length: a slip must be small compared to the word it damages. It is a `Warning` like the finding whose shape it borrows — a fault in the library rather than in the emitted tree — and this repository's CI runs `lint --deny error`, so it is visible without being fatal. And it was **built only once it could fail**: deferred through seven federation phases because the corpus declared no scopes, since a drift detector over an empty vocabulary arrives green and an artefact that cannot fail is one nobody notices is broken | *Edit distance 2 alone, as written* — flags `ruby`/`rust` and every pair of two-letter scopes; the muting begins with the first false positive. *A controlled vocabulary with an owner* — the owner is the single point of failure §12.1 already rejected one layer up, and nobody volunteers for it. *Flagging every scope used exactly once* — fires on the first rule of every genuinely new audience, which is how a vocabulary grows. *Reporting when two scopes are each used once* — there is no established spelling to have drifted from, and choosing which was intended is a judgement this check exists not to make. *An `Error` severity* — a heuristic that stops a build is the one people disable. *A string-similarity crate* — twenty lines against a dependency needing a price, for behaviour four tests fully specify |
| 2026-09-14 | **A scoped rule announces its audience, and the old byte-identity property is split rather than deleted** | The design's answer to "does a scope change what is emitted?" had been *no*, and the property `scope_never_reaches_an_emitted_path_or_body` made that provable over the whole space. It was also the reason a reader of `skills/domain-low-latency/SKILL.md` could not tell that the rule in front of them was written for Rust and Java engineers in particular — the field that decides who loads a rule was invisible to everyone who loaded it. `emit::audience_note` is the fix, and it is deliberately the **third member of an existing family** rather than a new mechanism: same `Option<String>` shape as `enforcement_note` and `recurrence_note`, same blockquote, same five splice sites, so it inherits the family's wiredness artifact instead of needing an argument of its own. `None` for an unscoped rule, so nothing an unscoped rule emits moved a byte, and no rule file changed. Spliced **first** of the three by explicit rank `[R:order-by-explicit-rank]`: is this mine, how firmly is it held, has it bitten. The old property was **split, not deleted** — `scope_never_reaches_an_emitted_path` keeps the half that was load-bearing (`Home` alone decides where a rule lands), and `scope_reaches_a_body_only_through_the_audience_note` replaces the body half with something stronger than "the note appears": it deletes the note from a scoped build and requires the remainder to be byte-identical to an unscoped one, so the announcement is provably the *whole* of the difference | ***Leaving it byte-identical*** — the status quo, and the reason the decision sat open in TODO.md since 2026-09-13: it makes the property easy to state and the emitted tree easy to reason about. Rejected because "easy to state" was being paid for by the reader, and the reader is who the emitted layer is for. *Putting the audience in front matter or a heading* — reaches the two vendors that have front matter and disappears in `AGENTS.md` and the copilot file, which is the wired-in-halves failure `[R:verdict-survives-the-channel]` names; the blockquote reaches all five identically. *Announcing "applies to all" on an unscoped rule* — a claim the rule does not make, and it would move bytes for all 52 unscoped rules to say nothing. *Only asserting the note appears* — satisfiable by an emitter that also leaked the audience into a path or a glob; only the whole-bytes subtraction closes that |
| 2026-09-16 | **`Status::Partial { by, uncovered, date }` — a rule can be partly held, and the status says which part is not** | Two rules reached the state where real controls covered part of a class and prose covered the rest, and neither existing status could say so. `Graduated` emits *"Also enforced by X"* into five instruction layers for ground X never claimed, and arms `RecurrenceAfterGraduation` — an `Error` that fails CI — against a recurrence in the uncovered part, so recording the truth would turn a warning red. `Active` throws away the fact that controls exist and were built. `Partial` reports like `Active` (prose still holds something, so a recurrence is still unheld) and annotates like `Graduated` (a reader is told which control holds what), and the two halves are kept apart by two **exhaustive** policy methods rather than by call-site matching: `prose_coverage` and `whole_class_claim`. That was the load-bearing part of the change — `lint` decided both questions with `matches!(status, Status::Active)`, which answers "no" for a new variant **silently**, so the variant would have shipped with partial rules quietly excluded from the unheld-recurrence finding and quietly flagged as retired references. Moving both into exhaustive matches on `Status` makes the next variant a compile error in exactly the places that must decide. `uncovered` is mandatory, in the variant, so "partly held but I will not say what is missing" is unconstructible; the alternative is a status that reads as coverage while covering nothing, which is `[R:guarantee-needs-a-reader]` wearing a status. `Finding::UnheldRecurrence` gained a `status` field in the same change, because its message said "is `active`" unconditionally and `Partial` made that false on arrival `[R:repair-the-lying-artefact]` | *Keeping the workaround* — three graduated rules already cram two controls into one `to` string with `" + "` and parenthetical halves, which renders acceptably and is what `verdict-survives-the-channel` used; rejected because it cannot stop `RecurrenceAfterGraduation` firing on the uncovered part, which is the actual harm. *De-stringifying `Destination` into a typed list of controls in the same change* — the literal reading of "single-destination", and a larger change: it migrates the on-disk format of eight graduated rules and touches the federation surface. Deliberately deferred as separable; this change fixes the harm, not the smell. *A boolean `partial: bool` on `Graduated`* — a flag that silently changes what a variant claims, and exactly the sentinel shape `[R:no-sentinel-values]` forbids. *Letting `lint` special-case the string* — puts the policy in the reader instead of the type, where the next reader will not find it |
| 2026-09-16 | **Every decision taken on a domain enum is an exhaustive policy method; `matches!` is reserved for identity tests** | `Status::Partial` shipped and was immediately found to be silently excluded from `UnheldRecurrence` and silently flagged as a `RetiredReference`, because both were decided by `matches!(status, Status::Active)` at the call site. Adding a variant does not break a `matches!`, an `if let`, or a named wildcard arm (`other =>`) — the new variant just takes the other branch, and no tool anywhere reports it. That is the same failure class as a sentinel value: a state nobody decided, reached by default. The sweep found nine production decisions across four enums and converted them into policy methods that match with no catch-all — `Status::{emittability, prose_coverage, whole_class_claim, is_withdrawn}`, `Authority::{provenance, is_editable}`, `Origin::recurrence_role`, `Home::federation`. Five would have answered *permissively*: a withheld citation as "not published yet", an unknown authority as contributable, withdrawn guidance as cacheable, an unknown authority joining a retirement poke, and a new `Origin` joining the *inert* bucket. Three sites asked the same `Authority` question in three spellings and two polarities, which is what made the class visible at all. The polarity accidents are the point: four of the nine were safe **only because the author happened to test the variant whose absence fails closed**, which is not a design | *Fixing only the five that fail open* — leaves four decisions nobody made, safe by an accident of polarity that the next edit can reverse, and leaves the class alive. *A clippy lint or a grep gate banning `matches!` on domain enums* — bans the legitimate identity test (`VerifyStatus::is_clean`) along with the policy, and a gate that must be muted for correct code is a muted gate. *Non-exhaustive enums with `#[non_exhaustive]`* — solves the reverse problem (downstream crates), and this crate has no downstream. *Leaving the predicates as `is_x()` helpers with `matches!` inside* — the call site reads identically, so the reviewer sees no difference, while the compiler still says nothing |
| 2026-09-16 | **A destination is a parsed `Controls` list; the on-disk format stays a string** | `Destination` was one non-empty string, and three rules had been encoding a *list* inside it — `"hook:a (pipeline half) + hook:b (edit half)"` — with a convention nothing enforced. The consequences were all silent: `to = "we added a test"` parsed and emitted happily while naming no control any tool could find, a mistyped prefix vanished from the federation's statistics, and `report::kinds_named` re-split the raw string **on whitespace**, which truncated every name containing one (`gate:verify.sh emoji_ban` read as `gate:verify.sh`). Parsing at the perimeter fixes all three at once and is the project's own `[R:parse-dont-validate]`: the string is the serialised form, `Controls` is the witness, and a malformed destination now stops the build like any other bad field. **The format did not change, and that was the point** — the charter values a neutral format that stays hand-editable and diff-friendly, the string is what travels between installs, and `Display` being the exact inverse of `parse` meant `verify` passed on all three output roots *without a rebuild*: the representation changed and not one emitted byte moved. That is a stronger statement about a refactor than any test, and it is available only because the format was left alone. `report`'s duplicate five-variant `Control` enum collapsed into `ControlKind`, which keeps the sealed published vocabulary while the *name* now sits in a separate field rather than being carefully avoided by a string reader | *Structured TOML — an array of inline tables* — self-documenting on disk, and it migrates nine rule files, changes the format that travels between installs, and makes the files less hand-editable; roughly three times the change for the same enforcement, since the grammar is enforced either way once something parses it. *A `[[status.to]]` section* — invalid here regardless: `status` sits mid-front-matter, so a table section would swallow every key after it, which is why `[[recurrence]]` lives at the end. *Keeping the string and teaching `report` a better reader* — fixes the truncation and leaves `to = "garbage"` emitting a graduation that names nothing. *Accepting `proptest:` as an alias for `property:`* — the old ad-hoc reader took both; only one can round-trip, and an alias that silently renames itself on rewrite is a diff nobody asked for |
| 2026-09-16 | **The `matches!` ban is a test that names its exemptions, not a grep that bans the idiom** | Converting the nine existing decisions into exhaustive policy methods made a *new variant* a compile error; it did nothing about a *new call site* written tomorrow, which the compiler cannot see and which is how all nine arrived. The obvious gate — grep for `matches!` over the domain enums — was rejected in the sweep as unbuildable, and the reason is worth keeping: it bans `VerifyStatus::is_clean`, a genuine identity test, and every `assert!(matches!(..))` in the suite, and **a gate that must be muted for correct code is a muted gate**. Two exemptions make it buildable and are the whole design: test code, and the enum's own module — because whoever adds a variant is editing that file with the variant list in front of them, while the decision three modules away is taken by someone who will never see the edit. It is a **test**, not a script under `scripts/`, on the same reasoning as the dependency and ledger gates: CI must remember to call a script, whereas a test is wired by construction. It found a tenth violation on its first run — `Rule::is_publishable` deciding `Federation` — which had survived the manual sweep precisely because it was *safe*, by the accident of which variant happened to be tested rather than by anyone deciding | *A clippy lint* — the right layer, and there is no lint for "this enum is a domain enum"; writing one is a `dylint` crate, which is a dependency and a build step for a check that fits in eighty lines. *Banning the idiom outright* — see above; the mute is the failure. *Checking only the four original enums* — leaves the five policy enums, which are the *answers*, so a call site re-deciding one has reintroduced the exact problem the policy removed. *Catching wildcard arms too* — needs a parser; the named form (`other =>`) defeats a substring search, so the gate states the gap in its own header rather than implying coverage it lacks |
| 2026-09-18 | **`Origin::Codified` carries an optional `SourceArtefact`, refused on every other origin** | The variant was deliberately payload-free, and the recorded reason was sound for the corpus it was written against: every codified rule had been ported out of the author's own always-loaded instruction file, so "codified without a source" was not an illegal state and the `incident` prose already said where it came from. It stopped being sound the first time a practice was codified from **someone else's** published artefact — an algorithm's original page, a specification, a paper — where the document is not a footnote to the provenance but *is* the provenance, and a reader who cannot reach it cannot check the rule against the thing that defines it `[R:source-practice-from-its-artefact]`. **Optional rather than mandatory is the whole design, and it is a different decision from the mandate's:** `Mandated` carries its approval because a mandate with no signer is an unenforced guarantee, and a codified rule naming no artefact is not the analogous defect — it is the common case, and fifty-seven committed rules depend on it staying free. What the payload buys is the other half — a source that is *present* is non-blank by construction, and a source on an origin with no use for one is a parse error rather than a field the parser quietly drops, exactly as `ApprovalWithoutMandate` already was. `Origin::parse` therefore takes origin, approval and source at **one perimeter** with no catch-all over the triple. The change also exposed a live defect it would have caused: `source` is the third authored field that travels on `contribute`, and the banned-terms scan list lived at the CLI call site, named two, and carried a comment asserting there were only two — so the enumeration moved to `Contribution::authored_texts`, beside the projection that decides what travels `[R:names-travel-with-the-quote]` | *A mandatory source on `Codified`* — makes the common case illegal and migrates twenty-two rule files to say something their `incident` already says. *A `Url` newtype* — the artefact is as often `~/.claude/CLAUDE.md, Reasoning & Methodology` or a book chapter as something with a scheme and a host, so it makes the commonest source unrepresentable while proving nothing about the uncommon one: a well-formed URL is not a reachable one. *A separate `CodifiedFrom(SourceArtefact)` variant* — doubles the match arms everywhere for two shapes of one origin, and `as_str` would still return `"codified"` for both. *An `Option<SourceArtefact>` field on `Rule` beside `origin`* — the same illegal-state-representable shape the approval was deliberately moved off, turning the invariant back into a check somebody must remember at every construction site. *Naming the type `Source`* — `SourceId` already means "which install a cache came from"; two meanings of "source" one module apart is the collision `[R:newtype-liberally]` exists to prevent |
| 2026-09-18 | **The source is announced in every emitted format, spliced last of the four notes** | The argument for adding the field was that a reader should be able to reach the artefact — and the reader who needs the citation is not the one reading `rules/<tag>.md`, who is the author, but the one holding the compiled skill. Provenance that never leaves the rule file serves nobody it was added for. Position is a decision rather than where it landed `[R:order-by-explicit-rank]`: `audience_note` asks whether the rule is yours, `enforcement_note` how firmly it is held, `recurrence_note` whether it has bitten — the reader's operational questions in the order they are asked — and provenance answers none of them, being what a reader follows when they want to verify, which is last and least often. The note is the **only** channel from a source into emitted text, held by a property that deletes it and requires the remainder byte-identical to a sourceless build, so provenance annotates and never rewrites a body, reorders around one, or decides whether a rule is emitted. **Recorded because it cuts against the feature:** a citation is a pointer into the reader's — or a model's — existing knowledge of the source, which helps where the rule agrees with it and misleads where the rule deliberately departs from it, as `[R:transient-state-is-not-a-terminal-state]` does from Vyukov's own `mpscq_pop`. The note names the artefact and never implies the rule agrees with it | *Provenance only, never emitted* — cheapest, and it serves the one reader who already has the rule file open while withholding it from every reader who does not. *Interpolating the artefact into the body* — makes the emitter read something the body did not say and breaks the single-channel property that makes the note safe. *Splicing it first, beside the audience note* — provenance is the least operational of the four and would displace the reader's actual first question. *A file-wide "no announcement" assertion in the wiredness property* — written first, and false: a concatenated file holds many rules, so it fails the moment one of them names an artefact. proptest produced the two-rule counterexample on the eighth case; the property now **counts** announcements per file, as the audience and recurrence twins already did |
| 2026-09-18 | **`relearn new` scaffolds the front matter; it does not author the rule, and it never replaces one** | Authoring a rule meant hand-writing eight required TOML keys, a date that must parse, a well-formed home, and two payload correspondences that are parse errors rather than warnings (`source` only on a codified rule, `approval` only on a mandate). None of that is the rule — the body and the incident are — but all of it is mechanical, and getting it wrong ends an authoring session in a diagnostic instead of a rule. The command takes every field at the perimeter, assembles the `Rule`, and hands it to the **same** witness, serializer and guarded write path `adopt` uses, so what it produces is byte-identical to the same rule typed by hand and `check` is the arbiter of both. Status is not an argument — a rule graduated or atticked on the day it is written is not a rule anyone learned anything from — and neither is `published_incident`, which is authored at contribution time and can be added by editing the file, because a rule file is hand-editable by design. **The one behaviour it adds is the create-never-replaces refusal, and it was found by running the binary rather than by the unit tests** (`[R:verify-through-production-path]`): `fsio::write_rule`'s guard refuses a target occupied by a *different* rule and permits overwriting the *same* one, which is right for `adopt` and silently discarded a rule's incident, recurrences and body here. The two commands want different answers to "may I write over this?", so the second question is asked in `new_rule` rather than by widening the shared guard and breaking `adopt` | *Taking the home as the slug `list --home` takes* — the obvious consistency, and wrong: `HomeSlug` is a one-way identity that turns a project path's separators into dashes, so `project-c-repo-sub` names a home it cannot reconstruct. A slug selects an existing home; it cannot construct one, and an inverse that guessed would silently invent a different project path. *Opening `$EDITOR` for the body* — `tests/solo_mode.rs` bans `Command::new`, and spawning a process is the back door to both the network and ambient state; `--body-file` is a path given on the command line like every other input. *Reading the date from a clock* — ambient state, on the same list and for the same reasons as a home-directory config file: it makes the command unreproducible and untestable without freezing time. *Defaulting a missing body or incident to a placeholder* — parses, emits, and says nothing, which is the silently useless rule the mandatory-provenance decision exists to prevent. *Widening `fsio::write_rule` to refuse same-rule overwrites* — breaks `adopt`, whose entire purpose is that write. *Boxing `Command`'s fields to satisfy `clippy::large_enum_variant`* — the lint's cost model is about an enum held in quantity; this one is built once per process from `argv` and destructured immediately, so boxing buys an allocation and an indirection for nothing measurable. Allowed with the reason recorded instead, because an unexamined lint obeyed is as much an unverified claim as one suppressed |
| 2026-09-18 | **A truncated skill description drops titles by `Status::instruction_reliance`, then recurrence, then tag** | `SkillDescription::build` walks its input and stops at the 1024-character cap, so whatever order it is handed decides which rules stay discoverable — and it was handed the caller's tag order. That is an incidental lexical sort standing in for a decision, which `[R:order-by-explicit-rank]` exists to forbid, and the consequence is not cosmetic: a dropped title is a rule the matcher cannot fire on, and for a home `LoadSemantics` makes `OnRequest` — every domain that is not a language, `low-latency` included — the description is the *only* path into a session, so a dropped rule may never load at all. The measurement that forced this: on a sixteen-rule low-latency home the three dropped titles were the three whose tags sort last, and they included the two independently ranked most valuable. Nothing about `p`, `t` or `v` says "least worth loading". The rank states the intent **keep the titles whose absence costs most**: a rule held by prose alone loses everything if its layer does not load, where a graduated rule still has the named control claiming its whole class and loses only the before-the-fact tuning. Recurrence descending is the second key, because a rule that has fired again is one this reader has already needed and the count is the corpus's only evidence of that; the tag is the third and does nothing but keep emission a deterministic function of the library. `instruction_reliance` is a **third** policy over `Status` rather than a reuse of `prose_coverage`, which is two-valued and buckets `Active` with `Partial` — correct for its own question and wrong here, since a partly-held rule has controls a wholly-unheld one does not. The rank orders the description and **not** the body, which stays in tag order so a reader can find a rule by name and a diff stays readable | *Leaving the tag order* — the status quo, and it is not an ordering decision at all: it changes silently the day a rule is renamed, and it demonstrably evicted the highest-value rules. *Reusing `prose_coverage`* — two-valued, so `Partial` and `Active` rank equal and a rule with real controls displaces one with none. *An explicit `priority` field in the rule format* — the only thing that would rank rules a reader actually disagrees about, and it is a format change, a migration, and a number every author must now argue about; deferred until something other than this truncation needs it. *Ranking by title length to fit the most titles* — maximises count and makes a long-titled important rule always lose, which is the same accident wearing a different coat. *Cutting rules until they fit* — tried, on the real drafts: sixteen rules to fourteen took the drop from five to three and the three still dropped were still the alphabetically-last, because pruning changes the count and never the selection. *Reordering the skill body too* — costs a reader the ability to find a rule by name, and every future diff its readability, to buy nothing the description does not already buy |
| 2026-09-19 | **The skill description lists tag bodies, not titles — the field is a trigger and never an inventory** | `description` is the only string a matcher reads when deciding whether to load a skill, it is capped at 1024 characters, and for every home `LoadSemantics` makes `OnRequest` it is the *only* path into a session. Listing titles cost sixty-odd characters each, so `global`'s thirty came to 1824 against that budget and the field carried fifteen of them plus `+15 more`; `domain-low-latency` carried thirteen of fifteen. A rule whose subject is absent cannot be matched on, so those rules were unreachable in the homes where it matters most. The same thirty tag bodies cost **749**, and every home now fits with room. The reason this is not compression, and not a heuristic: a tag is a field the author wrote, it is unique, it is the rule's identity, and it is already a keyword phrase — no articles, no verb of being, no qualifying clause — so nothing is inferred and nothing is discarded. The hyphens become spaces so a matcher sees separate word tokens rather than one identifier, and a human reading the skill list sees a phrase. **This is what the rank could not do.** `[R:order-by-explicit-rank]` was right and necessary — the alphabet was choosing which rules stayed reachable, and it had evicted the two independently ranked most valuable — but ordering decides *which* are dropped, never *whether*, and at 1824 against 1024 no ordering holds them. The rank stays for the residual case; the budget check is what closes the class | *Keeping titles and cutting rules until they fit* — measured on the real drafts: sixteen rules to fourteen took the drop from five to three and the same rules stayed invisible, because pruning changes the count and never the selection. *An explicit trigger field in the rule format* — the only thing that would carry vocabulary the tag does not, and it is a format change, a migration, and a second string every author must now write and keep true; deferred until something the tag cannot express actually needs it. *Deriving keywords from the title by stripping stop-words* — a heuristic over prose, fragile, and it would make the emitted bytes a function of an English word list rather than of the rule. *Emitting the tag verbatim, hyphens and all* — same budget, but the matcher sees one token and the reader sees an identifier. *Dropping the list and shipping only the lead sentence* — smallest field and the worst trigger: `Engineering discipline for working in low-latency` matches nothing a person would actually type, which is the defect that started this |
| 2026-09-19 | **Recurrence outranks reliance in the description's truncation rank; `contribute` scans every authored field; a documentation sample is checked as a rule** | Three open items closed together because each was a decision waiting rather than work waiting. **The rank keys were swapped.** Reliance-first answered "what does it cost for this title to be absent", which is a question about what *else* holds the rule, and is defensible read alone. It is inconsistent with everything around it: `lint` fails CI on an unheld recurrence, the session-start hook leads with recurrence, and the framework's whole argument is that recurrence is the number saying whether a rule works. Ranking a never-fired rule above a proven one inverted that in exactly one place, and the casualty was real — `[R:guarantee-needs-a-reader]`, `partial` with one recurrence, dropped from `global` while never-fired rules were kept. **The contribution scan was widened to `title` and `error_class`.** They had been withheld because widening would newly refuse contributions that pass today, and a gate that refuses previously-legal input gets muted; that trades a real exposure for a hypothetical one, since a contribution is a deliberate act behind `--confirm` and a term list, refusing one is the mechanism working, and nothing about a `title` prevents a product name being in it. Scanning four fields and reporting clean was worse than scanning five and occasionally refusing, because the report named what had been checked and not what had not. **And `tests/doc_samples.rs` now parses every `+++` block in the hand-authored documents**, closing a gap whose shape is more interesting than its size: a sample is prose to every reader and a rule to nobody, so `verify` (which reads only generated files) and `check` (which reads only `rules/`) were both structurally blind to it, and the README had been showing a document the parser refuses for months | *Leaving reliance first* — internally coherent, and it makes this one field weight recurrence opposite to every other consumer of it, which is the kind of inconsistency that is invisible until someone asks why a proven rule is missing. *A combined score* (reliance weighted against recurrence) — an arithmetic answer to a question that has a stated intent, and it makes "why was this dropped" unanswerable without running the formula. *Leaving `title` and `error_class` unscanned and recording the decision* — the honest version of the status quo, and it still ships a scan whose report is narrower than its name. *Scanning `tag` and `home` too* — `tag` is an identifier the corpus publishes in every emitted artifact, so scanning it flags names that are already public by design, and a project `home` never reaches a contribution at all, being withheld by `Home::federation`. *Globbing `docs/` for samples instead of naming files* — a glob silently starts covering a new document and silently stops covering a renamed one, and this gate's entire subject is a claim nobody was checking |
| 2026-09-19 | **The recurrence bucket of `1` is merged into `1-4`; a published bucket never denotes a single count** | The design specified `1 \| 2-4 \| 5-9 \| 10+` and the federation programme's own risk list warned, of that same boundary, that "a bucket of 1–1 is not a bucket". The design won where the two disagreed, so a spelling that publishes an exact count shipped — and the k-anonymity floor does not reach it, because that floor protects the **aggregate** while a raw report is a file in a public git repository, where `recurrences = "1"` is an exact count for one install, republished every month it appears. That is precisely the fingerprint the coarsening exists to prevent, arriving at the only size where a fingerprint is worth having. The analytical cost is near zero: the decay curve the aggregate is for needs orders of magnitude, and one-versus-four is not one. `Bucket::One` is gone rather than re-spelled, so the state cannot be reconstructed, and `aggregate::BUCKETS` drops to three entries — an incoming report using `1` or `2-4` is now `UnknownBucket` and is **refused**, which is the correct direction for a spelling whose problem is that it leaks. Nothing had been published, so nothing needed migrating | *Re-spelling `One` as `1-4` while keeping the variant* — two names for one range, and the variant would go on existing for a future author to map a count onto. *Keeping `1` and relying on the k-floor* — the floor is applied by the aggregate over installs; it has no view of a raw report, which is the artefact that sits in history. *Accepting the old spellings for backward compatibility* — there is nothing to be compatible with (no rule in this corpus is a cache and nothing has been published), and accepting `1` would mean the aggregate silently ingesting the leak it was changed to stop. *Widening further, to `1-9 \| 10+`* — buys little privacy over `1-4` and costs the mid-range signal the decay curve actually uses |
| 2026-09-19 | **The dependency decisions log is not retrofitted, and that is the decision rather than a deferral** | Eight of the nine dependencies predate `[R:price-every-dependency]` and have no row: `serde`, `clap`, `thiserror`, `anyhow`, `sha2`, `proptest`, `tempfile`, `trybuild`. Writing those rows now would mean reconstructing, years late, which alternatives were considered and rejected — and nobody remembers. The rule's own sibling names what that produces: the flattering version, a log that reads as deliberation and records a rationalisation. A decisions log whose entries were invented after the fact is worse than a log with gaps, because the gaps are visible and the inventions are not. So the eight stay unpriced, `tests/dependencies.rs` keeps exempting them **by name in a list that may only shrink**, and the exemption list is the honest record: it says exactly which decisions were never written down, and it cannot quietly grow to cover a new one. The rule binds every dependency added from here | *Retrofitting the eight* — the obvious tidy, and it manufactures provenance in the one document whose value is that it does not. *Dropping the exemption list and letting the gate fail* — an always-red gate is a muted gate, and this one is load-bearing for every future dependency. *Writing thin rows saying "predates the rule"* — nine rows of no content, which makes the log longer and tells a reader nothing the exemption list does not already say, while removing the one signal that distinguishes a priced decision from an unpriced one. *Making the manifest comments point at the log* — the sibling item, and it resolves with this one: eight of the nine have no row to point at, so the pointers would dangle |
| 2026-09-19 | **The open list is triaged into work, carried exposure, and decided — and the majority was never work** | `TODO.md` carried 46 open items, which read as a backlog and was not one. A sweep found three things sitting under `[ ]` that could not move: **federation** items, every one gated on a second install existing — there is one contributor and one subscriber, both George, so nothing about whether a rule mined from one person's sessions helps anyone else can be exercised, measured or validated; items belonging to **another repository**, which this tree cannot edit; and **paper and study-design** items waiting on a venue or a field site. Those are now `[!]`, a marker the charter already defined and nothing had used. Thirteen more were genuine decisions and are decided here, each recorded in place with its reasoning rather than ticked — the recurring verdict is *correct as it stands*, which is a close only when the reason is written down. Three were notes rather than tasks (a standing practice that duplicates a rule, a provenance record, a decision whose text already contained the decision). One was a real defect that had grown worse than its description: the copilot pack's prose named the repository discipline and the Rust set only, so 28 of 83 rules — every Java and low-latency rule — were undescribed; two sections were written and checked mechanically. Two stale figures were corrected against measurement: "three of eight graduations" was four, and "two such rules now exist" was fifteen. **What is left is one item.** The value is not the number: a backlog that cannot be worked is one nobody reads, which is how a real item hides among forty-five that are waiting for a second person | *Closing them by fiat* — the fastest, and it converts recorded exposure into unrecorded exposure, which is the failure the ledger exists to prevent. Every `[!]` keeps its full text and its own statement of what is exposed. *Deleting the untouchable items* — the same failure with tidier evidence. *Leaving all 46 under `[ ]`* — the status quo, and it is a lie of a different kind: it claims forty-six things are waiting for an afternoon. *A separate BACKLOG.md for the carried items* — a sixth standing document, which `[R:five-files-no-more]` forbids, to express what one character already expresses. *Marking them `[x]`* — they are not done; several will never be done here, and a reader deserves to know which |
| 2026-09-19 | **A second Copilot target, path-scoped — and every home gets a file even when the glob cannot narrow** | `emit::copilot` writes one `.github/copilot-instructions.md` holding the whole library, which Copilot applies to every file in the repository. Right for a small corpus; wrong at eighty-three rules, where eighteen Rust rules occupy context in a repository with no Rust in it — the P6 argument that motivated `--home`, reaching the one target that had no way to express it. Copilot also reads `.github/instructions/*.instructions.md`, each with an `applyTo:` glob, which is the load model `LoadSemantics` already derives for Cursor and the Claude rules layer, so this is reuse and not new modelling. **The design question is what to do with a home whose load model is not a glob**, and the answer differs from `emit::claude_rules` on purpose. That emitter skips an `OnRequest` domain because a `.claude/rules/` file cannot say "on request" *and* the Claude skill target carries those rules by description instead — the rule still reaches the assistant by another road. Copilot has no second road in this layer. Skipping `domain-low-latency` would leave fifteen rules reachable through no Copilot file at all, which is a correction lost: the failure at the top of `CLAUDE.md`. So every home is emitted, an unscopable one declares `applyTo: "**"` — the narrowest the format can express — and **says in its own header that it is unscoped and why**. That places the choice where it can actually be made: these files are copied into a repository one at a time, so the reader installs the domains they work in. A file nobody copies costs nothing; a rule that reaches no file cannot be copied at all. **Not in the default target set and not committed**: it is the alternative shape to `copilot` rather than an addition, installing both states every rule twice, and this repository's own `.github/` is read by Copilot, so committing both would double relearn's own instructions | *Skipping unscopable homes, as `claude_rules` does* — the consistent-looking answer, and it silently drops fifteen rules from a layer with no fallback. The two emitters differ because their targets differ, not because one is sloppier. *Emitting an unscopable home with no `applyTo` at all* — Copilot then applies it everywhere anyway, with nothing telling the reader that happened; the widening is the same and the disclosure is gone. *Folding it into `emit::copilot` behind a flag* — one emitter with two output shapes, where the `verify` pairing and the overwrite guard both assume a target names a fixed set of paths. *Adding it to the default targets* — the committed tree would carry both shapes, so anyone using Copilot on this repository would get every rule twice, which is the duplication the tool exists to prevent. *A `copilot-paths-pack/`, like the other two packs* — a third committed output root to demonstrate a target whose whole point is that you install one file at a time; the integration test over the real corpus is the stronger check anyway, because it asserts no rule is lost and a byte comparison would not |
| 2026-09-20 | **`[R:money-is-not-a-float]` is homed `global` with no `applies_to`, and ships with no control at all** | The error class is the representation, not the language: Java's `double`, C++'s `double` and Python's `float` fail identically and only the remedy's library name differs, so scoping to the `rust` audience would withhold the rule from the population that has the same defect under a different keyword — and the corpus now carries a real `domain-java` audience for whom the answer is `BigDecimal`. A rule declaring no `applies_to` is emitted under every audience, which is the behaviour wanted. **Deliberately `active` rather than `partial`:** `Partial` must name what its controls do not cover, and with no control the answer is "everything", which is a `NOTHING YET` row rather than a status. The rule is therefore layer 4 — the layer this very failure defeated, since a greenfield build has the model authoring the type layer, leaving layers 1 to 3 nothing to stand on. That limitation is stated in the rule body rather than left for a reader to infer, and the two controls that would close it are carried in `TODO.md` | *Homing it `domain=rust`* — the incident was a Rust build, and it would hide the rule from Java and C++ work with the identical defect. *A recurrence on `[R:newtype-liberally]`* — rejected: that rule's error class is two concepts becoming interchangeable, `Price(f64)` satisfies it completely, and its own worked example is `Miles(f64)` beside `Kilometers(f64)`, so it endorses the shape at issue. *A recurrence on `[R:make-illegal-states-unrepresentable]`* — rejected: a float holding a decimal is lossy, not contradictory, and stretching that class would blunt the one rule most often cited. *Marking it `partial` against the type discipline generally* — claims a control that does not exist, which is the false-graduation failure `Partial` was introduced to prevent |
| 2026-09-20 | **`global` is split: five disclosure rules become `domain=disclosure`, because a home's description is a budget and `global` had spent it** | `tests/description_reaches_every_rule.rs` — the budget check added 2026-09-19, whose stated job was to fail the day a home outgrew the field — failed at 38 global rules: the description reached **1010 of 1024** characters and dropped five subjects, two of them (`wired-artifact`, `source-practice-from-its-artefact`) rules that prose alone holds. The first remedy tried was to let a **graduated** rule's subject be dropped, on the ground that a named control still claims its class. Measured and rejected: only two of the 38 are graduated, so the prose-reliant 36 cost **1010 + 74 of lead = 1084** with both already excluded — over by 60 before any choice is made. Trimming the lead buys ~35 and does not reach either. **The decisive correction was to a premise, not to the arithmetic.** Splitting had been argued against twice on the ground that a non-language domain is `OnRequest` while `global` is always-loaded — and `global` is not: `emit::claude_rules` emits project and domain homes and *not* `global` (`project_and_domain_homes_each_get_a_file_but_global_does_not`), so `global` exists only as a skill and its description is the **only** route into a session for every rule in it. Splitting therefore costs nothing in load semantics, and two focused descriptions match better than one carrying 38 subjects across five unrelated topics. `[R:check-the-claim-you-inherit]`, written the same day, against its own author. The five moved are a coherent audience — what may leave for a public reader — and `global` lands at 33 rules, **956 / 1024** | *Relaxing the budget check* — deletes a failing gate to make a change pass, and the gate was right. *Accepting truncation with a remainder counter* — reverts the 2026-09-19 guarantee that every rule is named, and leaves two prose-only rules unmatchable and the number growing. *Moving the disclosure rules to `org=`* — expresses ownership correctly and hides nothing, since the repository is public either way, and it would withhold them from federation for no gain. *Splitting off the project-document rules as well* — the larger, better-headroom split, not taken here: the natural name collides with an existing hand-authored `project-discipline` skill, and that collision is a question about which of the two owns the five-files discipline, which is worth its own pass rather than a naming dodge |
| 2026-09-21 | **The interim control for `[R:money-is-not-a-float]` is a script taking roots, not a `cargo test` — and it is built to be copied out of this repository** | This supersedes the closing clause of the row above: the rule no longer ships with no control, and its status moved `active` → `partial` the moment one existed, because `Partial` can now name a remainder. The shape was the real decision. `scripts/verify-dependencies.sh` argues the opposite way and is right to — its subject is *this* repository's manifests, so a test is wired by construction and cannot arrive disarmed. This check's subject is a **financial application's source**, which is not this tree and never will be: a test inside `relearn` could only ever scan `relearn`, where a monetary field will never appear, and would report green by measuring nothing — a control placed where the thing it controls is absent, `[R:wired-artifact]`. So the roots are command-line arguments, the script is meant to be copied into the repository it is about with its fixtures, and FEATURES.md carries the two-command install. Running it here is not the point of it; it keeps the detector exercised against a real tree at every push rather than against fixtures alone. Three further choices fall out. It reads the **concept** — identifier components, de-pluralised, against the rule's vocabulary — so `costume_weight` and `totality` do not hit, because a substring matcher is a muted matcher within a week. It covers the tuple newtype `struct Price(f64)` as well as the field, since that shape satisfies `[R:newtype-liberally]` completely and is still exactly the defect. And it carries `--self-test` over two committed fixtures, whose **exit codes are read as well as their output**, so a detector that has stopped detecting fails before it can certify a tree `[R:guarantee-needs-a-reader]`. Wired to `.githooks/pre-push` *and* to CI: unlike the banned-name gate it needs no machine-local list, so a fork can run it | *A `cargo test`, for the wiring-by-construction the dependency gate enjoys* — the tempting symmetry, and it would scan the one tree the defect cannot appear in. *A grep over `f64`* — flags every latency and ratio in the tree, goes red on day one, gets muted, and then holds nothing; the `emoji_ban` half of `[R:verify-the-glyph-exists]` can ban whole codepoint blocks outright only because no legitimate use exists, and floats are not like that. *Struct fields only, as `TODO.md` originally specified* — would pass `struct Price(f64)`, the example the rule body names by name. *No `// money-scan: measured` escape* — a check with no escape is a check that goes red on the first honest false positive and is then switched off; the escape is what keeps it armed. *Shipping it only as a `copilot-pack`-style folder, never run here* — an artefact nobody executes, which is how a gate rots between the day it is written and the day it is needed. *Waiting for the scaffolding crate, the item that actually closes the class* — it remains open in `TODO.md` and it is the fix; leaving the rule at layer 4 in the meantime keeps the whole class on the layer this very incident defeated |
